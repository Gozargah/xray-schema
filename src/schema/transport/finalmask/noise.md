Noise camouflage for UDP.

Fields:

- `reset`: reset marker for the noise sequence.
- `noise`: array of noise packets.

Each noise packet can set random payload size, byte range, packet data, packet type, and delay.
