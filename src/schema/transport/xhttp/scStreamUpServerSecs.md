Stream-up, server-only. Default is `"20-80"` seconds; the server sends `xPaddingBytes` periodically to keep downstream alive.

Set to `-1` to disable; the server may delay response headers to match earlier behavior.
