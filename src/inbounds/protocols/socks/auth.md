Authentication method. Supports `"noauth"` (anonymous) and `"password"` (user/password).

When using `"password"`, HTTP requests sent to this inbound also require the same account and password.

Default value is `"noauth"`.
