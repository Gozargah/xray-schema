Maximum time (seconds) a connection can be reused. After this time, it will receive no new requests and closes after the last request.

Default (when all XMUX fields are `0`) is `"1800-3000"`, randomized. Otherwise default is `0` (no limit).
