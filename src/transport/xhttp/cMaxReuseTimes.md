Maximum reuse count for a connection. After reaching this number, the connection receives no new requests and closes after the last internal request.

Default is `0` (no limit). Supports range strings.
