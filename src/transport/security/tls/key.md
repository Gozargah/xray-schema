An array of strings representing the private-key content, in the same format as PEM-encoded private keys. Choose either `key` or `keyFile`.

### TIP

When `usage` is `"verify"`, both `keyFile` and `key` may be empty.
