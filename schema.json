{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "properties": {
    "version": {
      "type": "object",
      "properties": {
        "min": { "$ref": "#/definitions/__schema0" },
        "max": { "$ref": "#/definitions/__schema1" }
      },
      "additionalProperties": false,
      "markdownDescription": "Controls the version on which this config can run. This prevents accidental running on unexpected client versions when sharing the config. The client will check if the current version matches this requirement at runtime.\n\n```json\n\"version\": {\n  \"min\": \"25.8.3\",\n  \"max\": \"\n}\n```\n\nBoth `min` and `max` are optional. Not setting them or leaving them empty means no restrictions. It does not need to be an actual existing version, as long as it complies with the Xray version syntax x.y.z.\n\n**25.8.3** is the version where Xray added this feature. Setting a version lower than this is meaningless (older versions will not check it).\n\n---\n\n[Documentation ↗](https://xtls.github.io/en/config/version.html)\n"
    },
    "log": {
      "type": "object",
      "properties": {
        "access": { "$ref": "#/definitions/__schema2" },
        "error": { "$ref": "#/definitions/__schema4" },
        "loglevel": { "$ref": "#/definitions/__schema6" },
        "dnsLog": { "$ref": "#/definitions/__schema8" },
        "maskAddress": { "$ref": "#/definitions/__schema10" }
      },
      "required": ["dnsLog"],
      "additionalProperties": false,
      "markdownDescription": "Log configuration controls how Xray outputs logs.\n\nXray has two types of logs:\n  1. access logs\n  2. error logs\n\nYou can configure the output method for each type independently.\n\n---\n\n[Documentation ↗](https://xtls.github.io/en/config/log.html)\n"
    },
    "api": {
      "type": "object",
      "properties": {
        "tag": { "$ref": "#/definitions/__schema12" },
        "listen": { "$ref": "#/definitions/__schema13" },
        "services": { "$ref": "#/definitions/__schema15" }
      },
      "required": ["tag"],
      "additionalProperties": false,
      "markdownDescription": "The API interface configuration provides [gRPC](https://grpc.io/)-based API interfaces for remote procedure calls.\n\n\nYou can enable the interface through the `api` configuration module. When the `api` configuration is enabled, Xray will automatically build an outbound proxy with the same name as the `tag`. You must manually route all API inbound connections to this outbound proxy via [Routing Configuration](https://xtls.github.io/en/config/routing.html). Please refer to [Relevant Configuration](https://xtls.github.io/en/config/api.html#relevant-configuration) in this section.\n\n\nSince [v1.8.12](https://github.com/XTLS/Xray-core/releases/tag/v1.8.12), a simplified configuration mode is supported. You only need to configure the ApiObject, without needing to configure `inbounds` and `routing`. However, when using the simplified configuration, the traffic statistics feature does not count the traffic of API inbound connections.\n\n---\n\n[Documentation ↗](https://xtls.github.io/en/config/api.html)\n",
      "isTrusted": true
    },
    "dns": {
      "type": "object",
      "properties": {
        "hosts": { "$ref": "#/definitions/__schema17" },
        "servers": { "$ref": "#/definitions/__schema19" },
        "tag": { "$ref": "#/definitions/__schema28" },
        "clientIP": { "$ref": "#/definitions/__schema30" },
        "queryStrategy": { "$ref": "#/definitions/__schema32" },
        "disableCache": { "$ref": "#/definitions/__schema34" },
        "disableFallback": { "$ref": "#/definitions/__schema36" },
        "disableFallbackIfMatch": { "$ref": "#/definitions/__schema38" },
        "useSystemHosts": { "$ref": "#/definitions/__schema40" },
        "enableParallelQuery": { "$ref": "#/definitions/__schema42" },
        "serveStale": { "$ref": "#/definitions/__schema44" },
        "serveExpiredTTL": { "$ref": "#/definitions/__schema46" }
      },
      "additionalProperties": { "$ref": "#/definitions/__schema48" },
      "markdownDescription": "Built-in DNS server. If this item is not configured, the system DNS settings are used.\n\nThe built-in DNS module in Xray has three main purposes:\n\n- **Routing Phase:** Resolves domain names to IPs and matches rules based on the resolved IPs for traffic splitting. Whether to resolve the domain and split traffic depends on the `domainStrategy` setting in the routing configuration module. The built-in DNS server is used for DNS queries only when the following two values are set:\n  - `\"IPIfNonMatch\"`: When a domain is requested, Xray attempts to match it against the `domain` rules in the routing configuration. If no match is found, the built-in DNS server is used to resolve the domain, and the returned IP address is used to match against IP routing rules.\n  - `\"IPOnDemand\"`: When any IP-based rule is encountered during matching, the domain is immediately resolved to an IP for matching.\n\n- **Resolving Target Addresses for Connections:**\n  - For example, in a `freedom` outbound, if `domainStrategy` is set to `UseIP`, requests sent from this outbound will first resolve the domain to an IP using the built-in server before connecting.\n  - For example, in `sockopt`, if `domainStrategy` is set to `UseIP`, system connections initiated by this outbound will first resolve to an IP using the built-in server before connecting.\n\n- **TUN/Transparent Proxy DNS Traffic Hijacking:** Combines routing with the DNS outbound to hijack DNS traffic into this module; or directly exposes port 53 to act as a recursive DNS server.\n\n#### TIP 1\nThe DNS server enters the routing system for matching by default unless it contains `+local`. When using domain names within it, be aware of potential routing loops; `hosts` may help.\n\n#### TIP 2\nOnly basic IP queries (A and AAAA records) are supported. CNAME records will be queried repeatedly until an A/AAAA record is returned. Other queries will not enter the built-in DNS server; instead, they may be discarded or transparently forwarded to other servers depending on your outbound configuration.\n\n## DNS Processing Flow\n\nThe domain first undergoes a Hosts mapping check (see the `hosts` field). If the required IP is not found, the DNS server is used for the query.\n\nThe core then begins to build a list of servers, sorting them according to the requested domain based on the following rules.\n\n- Build List 1: Contains servers where the `domains` field successfully matches the requested domain, in the order they appear in the configuration file.\n- Check `disableFallback`: If true, skip building List 2.\n- Check `disableFallbackIfMatch`: If true and List 1 is not empty, skip building List 2.\n- Build List 2: Contains servers not in List 1 where `skipFallback` is not true, in the order they appear in the configuration file.\n- Final Server List = List 1 + List 2.\n\nNote: Any DNS server with `FinalQuery` set to true will directly truncate the subsequent parts of the list.\n\nWhen executing a DNS query, the core will query the servers in the Final Server List sequentially. It filters the results using `expectedIPs` and `unexpectedIPs`; if the result is empty after filtering, it attempts the next server in the list. (Behavior differs slightly when `enableParallelQuery` is true; see its field description for details.)\n\n\n[Documentation ↗](https://xtls.github.io/en/config/dns.html)\n"
    },
    "routing": {
      "type": "object",
      "properties": {
        "domainStrategy": { "$ref": "#/definitions/__schema49" },
        "rules": { "$ref": "#/definitions/__schema51" },
        "balancers": { "$ref": "#/definitions/__schema71" }
      },
      "additionalProperties": { "$ref": "#/definitions/__schema89" },
      "markdownDescription": "The routing module can send inbound data through different outbound connections based on different rules, achieving the purpose of on-demand proxying.\n\nA common usage is splitting traffic between domestic and foreign destinations. Xray can determine the region of the traffic through internal mechanisms and then send them to different outbound proxies.\n\nFor a more detailed analysis of the routing function: [Analysis of Routing (Part 1)](https://xtls.github.io/en/document/level-1/routing-lv1-part1.html).\n\n---\n\n[Documentation ↗](https://xtls.github.io/en/config/routing.html)\n"
    },
    "policy": {
      "type": "object",
      "properties": {
        "levels": { "$ref": "#/definitions/__schema90" },
        "system": { "$ref": "#/definitions/__schema100" }
      },
      "additionalProperties": false,
      "markdownDescription": "Local policy allows setting different user levels and corresponding policy settings, such as connection timeout settings. Every connection processed by Xray corresponds to a user, and different policies are applied according to the user's level.\n\n---\n\n[Documentation ↗](https://xtls.github.io/en/config/policy.html)"
    },
    "stats": {
      "type": "object",
      "properties": {},
      "additionalProperties": { "$ref": "#/definitions/__schema106" },
      "markdownDescription": "Currently, statistics do not require any parameters. As long as the StatsObject item exists, internal statistics are enabled.\n\nAfter enabling statistics, you only need to enable the corresponding items in Policy to collect the corresponding data."
    },
    "metrics": {
      "anyOf": [{ "$ref": "#/definitions/__schema107" }, { "$ref": "#/definitions/__schema109" }],
      "markdownDescription": "A more direct (and hopefully better) way to export statistics.\n\n---\n\n[Documentation ↗](https://xtls.github.io/en/config/metrics.html)"
    },
    "observatory": {
      "type": "object",
      "properties": {
        "subjectSelector": { "$ref": "#/definitions/__schema111" },
        "probeUrl": { "$ref": "#/definitions/__schema113" },
        "probeInterval": { "$ref": "#/definitions/__schema115" },
        "enableConcurrency": { "$ref": "#/definitions/__schema116" }
      },
      "required": ["subjectSelector", "probeUrl", "probeInterval"],
      "additionalProperties": false,
      "markdownDescription": "The Observatory component uses HTTPing to probe the connection status of outbound proxies. The observation results can be used by other components, such as the Load Balancer. Currently, there are two types: [observatory](https://xtls.github.io/en/config/observatory.html#observatoryobject) (Background Connection Observatory) and [burstObservatory](https://xtls.github.io/en/config/observatory.html#burstobservatoryobject) (Burst Connection Observatory). Choose one according to your needs."
    },
    "burstObservatory": {
      "type": "object",
      "properties": {
        "subjectSelector": { "$ref": "#/definitions/__schema118" },
        "pingConfig": { "$ref": "#/definitions/__schema120" }
      },
      "required": ["subjectSelector"],
      "additionalProperties": false,
      "markdownDescription": "The Observatory component uses HTTPing to probe the connection status of outbound proxies. The observation results can be used by other components, such as the Load Balancer. Currently, there are two types: [observatory](https://xtls.github.io/en/config/observatory.html#observatoryobject) (Background Connection Observatory) and [burstObservatory](https://xtls.github.io/en/config/observatory.html#burstobservatoryobject) (Burst Connection Observatory). Choose one according to your needs."
    },
    "reverse": {
      "anyOf": [{ "$ref": "#/definitions/__schema128" }, { "$ref": "#/definitions/__schema139" }],
      "markdownDescription": "A reverse proxy can forward traffic from the server side to the client side, effectively performing reverse traffic forwarding.\n\nThis reverse proxy is a general-purpose reverse proxy (it does not limit the proxy protocol type). The configuration is more complex. Do not confuse it with the VLESS simplified reverse configuration (refer to the relevant sections in the VLESS inbound/outbound documentation).\n\nIts underlying protocol is Mux.cool, but the direction is reversed: the server initiates requests to the client.\n\nThe general working principle of the reverse proxy is as follows:\n\n- Assume there is a web server on Host A. This host does not have a public IP and cannot be accessed directly from the public internet. There is another Host B, which is accessible from the public internet. We need to use B as the entry point to forward traffic from B to A.\n  - Configure Xray on Host B to receive external requests; this is called the `portal`.\n  - Configure Xray on Host A to bridge the forwarding from B to the web server; this is called the `bridge`.\n\n- `bridge`\n  - The `bridge` actively establishes a connection to the `portal` to register a reverse tunnel. The destination address (domain) of this connection can be defined by the user.\n  - After receiving public traffic forwarded by the `portal`, the `bridge` sends it intact to the web server on Host A. Naturally, this step requires configuration in the routing module.\n  - Upon receiving a response, the `bridge` returns the response intact to the `portal`.\n\n- `portal`\n  - If the `portal` receives a request and the domain matches, it indicates response data sent by the `bridge`. This connection will be used to establish the reverse tunnel.\n  - If the `portal` receives a request and the domain does _not_ match, it indicates a connection from a public user. This connection data will be forwarded to the bridge.\n\n- The `bridge` performs dynamic load balancing based on traffic volume.\n\nAs mentioned above, the reverse proxy has [Mux](https://xtls.github.io/en/development/protocols/muxcool.html) enabled by default. Please do not enable Mux again on the outbounds used by it.\n\nThe reverse proxy function is currently in the testing stage and may have some issues.\n",
      "deprecated": true,
      "deprecationMessage": "This feature has been deprecated. Please use the VLESS reverse proxy."
    },
    "inbounds": { "minItems": 1, "type": "array", "items": { "$ref": "#/definitions/__schema140" } }
  },
  "required": ["inbounds"],
  "additionalProperties": {},
  "definitions": {
    "__schema0": { "type": "string" },
    "__schema1": { "type": "string" },
    "__schema2": {
      "markdownDescription": "The file path for the access log.\n\nIts value must be a valid file path, such as\n\n- `\"/var/log/Xray/access.log\"` (Linux)\n- `\"C:\\\\Temp\\\\Xray\\\\_access.log\"` (Windows)\n\nWhen this item is unspecified or empty, logs are output to stdout.\n\nSpecial value `none`: disables the access log.\n",
      "allOf": [{ "$ref": "#/definitions/__schema3" }]
    },
    "__schema3": {
      "default": "",
      "anyOf": [{ "type": "string" }, { "type": "string", "const": "none" }]
    },
    "__schema4": {
      "markdownDescription": "The file path for the error log.\n\nIts value must be a valid file path, such as\n- `\"/var/log/Xray/error.log\"` (Linux)\n- `\"C:\\\\Temp\\\\Xray\\\\_error.log\"` (Windows).\n\nWhen this item is unspecified or empty, logs are output to stdout.\n\nSpecial value `none`: disables the error log.\n",
      "allOf": [{ "$ref": "#/definitions/__schema5" }]
    },
    "__schema5": {
      "default": "",
      "anyOf": [{ "type": "string" }, { "type": "string", "const": "none" }]
    },
    "__schema6": {
      "markdownDescription": "The level of the error log, indicating the information that needs to be recorded. The default value is `\"warning\"`.\n\n- `\"debug\"`: Output information used for debugging. Includes all \"info\" content.\n- `\"info\"`: Runtime status information, etc., which does not affect normal usage. Includes all `\"warning\"` content.\n- `\"warning\"`: Information output when issues occur that do not affect normal operation but may impact user experience. Includes all `\"error\"` content.\n- `\"error\"`: Xray encountered a problem where it cannot operate normally and requires immediate resolution.\n- `\"none\"`: Do not record any content.\n",
      "allOf": [{ "$ref": "#/definitions/__schema7" }]
    },
    "__schema7": {
      "default": "warning",
      "type": "string",
      "enum": ["debug", "info", "warning", "error", "none"]
    },
    "__schema8": {
      "default": false,
      "markdownDescription": "Whether to enable DNS query logs, for example: `DOH//doh.server got answer: domain.com -> [ip1, ip2] 2.333ms`\n",
      "allOf": [{ "$ref": "#/definitions/__schema9" }]
    },
    "__schema9": { "type": "boolean" },
    "__schema10": {
      "markdownDescription": "IP address mask. When enabled, it automatically replaces IP addresses appearing in the log to protect privacy when sharing logs.\n\nThe default is `'empty'` (disabled).\n\nCurrently, the available levels are `'quarter'`, `'half'`, and `'full'`.\n\nThe masking formats correspond as follows:\n\n- ipv4: `1.2.*.*` , `1.*.*.*` , `[Masked IPv4]`\n- ipv6: `1234:5678::/32` , `1234::/16` , `[Masked IPv6]`\n\nFor more specific requirements, you can use a custom format such as `/16+/32`. The format defines the number of bits to keep unmasked; the first number is for IPv4 and the second for IPv6. Note that the IPv4 value must be divisible by 8. Using /32 (IPv4) or /128 (IPv6) means no masking, while /0 will display as `[Masked IPv4/IPv6]`.\n\nHelp us improve this page on GitHub!\n",
      "allOf": [{ "$ref": "#/definitions/__schema11" }]
    },
    "__schema11": {
      "default": "",
      "anyOf": [
        { "type": "string", "const": "" },
        { "type": "string", "enum": ["quarter", "half", "full"] },
        {
          "type": "string",
          "pattern": "^\\/(?:0|8|16|24|32)\\+\\/(?:12[0-8]|1[01][0-9]|[0-9]{1,2})$"
        }
      ]
    },
    "__schema12": {
      "type": "string",
      "markdownDescription": "The identifier of the outbound proxy."
    },
    "__schema13": {
      "markdownDescription": "The IP and port for the API service to listen on. This is an optional configuration item.\n\nIf this item is omitted, you need to add `inbounds` and `routing` configurations according to the example in [Relevant Configuration](https://xtls.github.io/en/config/api.html#relevant-configuration) below.\n",
      "allOf": [{ "$ref": "#/definitions/__schema14" }]
    },
    "__schema14": { "type": "string" },
    "__schema15": {
      "markdownDescription": "The list of enabled APIs. See [API List](https://xtls.github.io/en/config/api.html#supported-api-list) for available values.",
      "allOf": [{ "$ref": "#/definitions/__schema16" }]
    },
    "__schema16": {
      "type": "array",
      "items": {
        "anyOf": [
          {
            "type": "string",
            "const": "HandlerService",
            "markdownDescription": "APIs for modifying inbound and outbound proxies. Available functions are as follows:\n- Add a new inbound;\n- Add a new outbound;\n- Remove an existing inbound;\n- Remove an existing outbound;\n- List outbounds;\n- List inbounds;\n- Add a user to an inbound (supports VMess, VLESS, Trojan, Shadowsocks only);\n- Remove a user from an inbound (supports VMess, VLESS, Trojan, Shadowsocks only);\n"
          },
          {
            "type": "string",
            "const": "RoutingService",
            "markdownDescription": "APIs for adding, removing, replacing routing rules, and querying balancer statistics. Available functions are as follows:\n\n- `adrules`: Add or replace routing configuration\n- `rmrules`: Remove routing rules\n- `sib`: Disconnect connections from a source IP\n- `bi`: Query balancer statistics\n- `bo`: Force balancer to select a specific `outboundTag`\nYou can use commands like `./xray help api` bi to query specific usage.\n"
          },
          {
            "type": "string",
            "const": "LoggerService",
            "markdownDescription": "Supports restarting the built-in Logger, which can be used with `logrotate` to perform operations on log files."
          },
          {
            "type": "string",
            "const": "StatsService",
            "markdownDescription": "Built-in data statistics service. See [Statistics](https://xtls.github.io/en/config/stats.html) for details."
          },
          {
            "type": "string",
            "const": "ReflectionService",
            "markdownDescription": "Allows gRPC clients to retrieve the list of APIs on the server."
          }
        ]
      }
    },
    "__schema17": {
      "markdownDescription": "A static IP mapping. The value consists of entries in the form `\"domain\": \"address\"` or `\"domain\": [\"address 1\", \"address 2\"]`. Each address may be an IP or a domain name. When resolving a domain, the core will check all mapping entries and return all matched IP addresses. If nothing matches, the DNS query phase is entered.\n---\nThe mapping target may be a domain name. When the core finishes matching and the mapping contains domain name(s), the behavior is slightly different:\n---\n- If the mapping contains both IP addresses and domain names, the domain names are removed and only the IP addresses are returned.\n- If the mapping contains several domain names, the result is ambiguous: the match fails, is treated as a miss, and the DNS query phase is entered.\n- If the mapping contains exactly one domain name, that domain will be fed back into the Hosts module for recursive resolution, repeating the above steps with a maximum recursion depth of 5.\n- If the above recursive resolution yields no IPs, and the final resolution result contains exactly one domain name, that domain replaces the original requested domain and is sent to the DNS query phase.\n---\nThe matching format (`domain:`, `full:`, etc.) is the same as the domain in the commonly used [Routing System](https://xtls.github.io/en/config/routing.html#ruleobject). The difference is that without a prefix, it defaults to using the `full:` prefix (similar to the common hosts file syntax).\n",
      "allOf": [{ "$ref": "#/definitions/__schema18" }]
    },
    "__schema18": {
      "type": "object",
      "propertyNames": { "type": "string" },
      "additionalProperties": {
        "anyOf": [{ "type": "string" }, { "type": "array", "items": { "type": "string" } }]
      }
    },
    "__schema19": {
      "markdownDescription": "A list of DNS servers. Two types are supported: DNS address (string format) and [DnsServerObject](https://xtls.github.io/en/config/dns.html#dnsserverobject).\n\nWhen the value is `\"localhost\"`, it indicates using the local machine's preset DNS configuration.\n---\nWhen the value is a DNS `\"IP:Port\"` address, such as `\"8.8.8.8:53\"`, Xray will use the specified UDP port of this address for DNS queries. The query follows routing rules. If no port is specified, port 53 is used by default.\n---\nWhen the value is in the form of `\"tcp://host:port\"`, such as `\"tcp://8.8.8.8:53\"`, Xray will use `DNS over TCP` for queries. The query follows routing rules. If no port is specified, port 53 is used by default.\n---\nWhen the value is in the form of `\"tcp+local://host:port\"`, such as `\"tcp+local://8.8.8.8:53\"`, Xray will use `TCP Local Mode (TCPL)` for queries. This means the DNS request will **not** pass through the routing component but will request directly via the Freedom outbound to reduce latency. If no port is specified, port 53 is used by default.\n---\nWhen the value is in the form of `\"https://host:port/dns-query\"`, such as `\"https://dns.google/dns-query\"`, Xray will use `DNS over HTTPS` (RFC8484, abbreviated as DOH) for queries. Some providers have certificates for IP aliases, so you can write the IP directly, such as `https://1.1.1.1/dns-query`. Non-standard ports and paths can also be used, such as `\"https://a.b.c.d:8443/my-dns-query\"`.\n---\nWhen the value is in the form of `\"h2c://host:port/dns-query\"`, such as `\"h2c://dns.google/dns-query\"`, Xray will use the `DNS over HTTPS` request format but will send the request in cleartext h2c. This cannot be used directly; in this case, you need to configure a Freedom outbound + streamSettings with TLS to wrap it into a normal DOH request. This is used for special purposes, such as customizing the SNI of DOH requests or using utls fingerprints.\n---\nWhen the value is in the form of `\"https+local://host:port/dns-query\"`, such as `\"https+local://dns.google/dns-query\"`, Xray will use `DOH Local Mode (DOHL)` for queries. This means the DOH request will **not** pass through the routing component but will request directly via the Freedom outbound to reduce latency. Generally suitable for server-side use. Non-standard ports and paths can also be used.\n---\nWhen the value is in the form of `\"quic+local://host\"`, such as `\"quic+local://dns.adguard.com\"`, Xray will use `DNS over QUIC Local Mode (DOQL)` for queries. This means the DNS request will **not** pass through the routing component but will request directly via the Freedom outbound. This method requires the DNS server to support DNS over QUIC. By default, port 853 is used for queries, and non-standard ports can be used.\n---\nWhen the value is `fakedns`, the FakeDNS feature will be used for queries.\n---\n\n### TIP 1\nWhen using `localhost`, the local machine's DNS requests are not controlled by Xray. Additional configuration is required to forward DNS requests through Xray.\n\n### TIP 2\nThe DNS clients initialized by different rules will be shown in the Xray startup logs at the `info` level, such as `local DOH`, `remote DOH`, and `udp` modes.\n\n\n### TIP 3\n(v1.4.0+) You can enable DNS query logging in [Log](./log.md).\n\n",
      "allOf": [{ "$ref": "#/definitions/__schema20" }]
    },
    "__schema20": {
      "type": "array",
      "items": {
        "anyOf": [
          { "type": "string" },
          {
            "type": "object",
            "properties": {
              "address": {
                "type": "string",
                "markdownDescription": "When the value is `\"localhost\"`, it indicates using the local machine's preset DNS configuration.\n---\nWhen the value is a DNS `\"IP\"` address, such as `\"8.8.8.8\"`, Xray will use the specified UDP port of this address for DNS queries. The query follows routing rules. Defaults to port 53.\n---\nWhen the value is in the form of `\"tcp://host\"`, such as `\"tcp://8.8.8.8\"`, Xray will use `DNS over TCP` for queries. The query follows routing rules. Defaults to port 53.\n---\nWhen the value is in the form of `\"tcp+local://host\"`, such as `\"tcp+local://8.8.8.8\"`, Xray will use `TCP Local Mode (TCPL)` for queries. This means the DNS request will **not** pass through the routing component but will request directly via the Freedom outbound to reduce latency. If no port is specified, port 53 is used by default.\n---\nWhen the value is in the form of `\"https://host:port/dns-query\"`, such as `\"https://dns.google/dns-query\"`, Xray will use `DNS over HTTPS` (RFC8484, abbreviated as DOH) for queries. Some providers have certificates for IP aliases, so you can write the IP directly, such as `https://1.1.1.1/dns-query`. Non-standard ports and paths can also be used, such as `\"https://a.b.c.d:8443/my-dns-query\"`.\n---\nWhen the value is in the form of `\"https+local://host:port/dns-query\"`, such as `\"https+local://dns.google/dns-query\"`, Xray will use `DOH Local Mode (DOHL)` for queries. This means the DOH request will **not** pass through the routing component but will request directly via the Freedom outbound to reduce latency. Generally suitable for server-side use. Non-standard ports and paths can also be used.\n---\nWhen the value is in the form of `\"quic+local://host:port\"`, such as `\"quic+local://dns.adguard.com\"`, Xray will use `DOQ Local Mode (DOQL)` for queries. This means the DNS request will **not** pass through the routing component but will request directly via the Freedom outbound. This method requires the DNS server to support DNS over QUIC. By default, port 853 is used for queries, and non-standard ports can be used.\n---\nWhen the value is `fakedns`, the FakeDNS feature will be used for queries.\n---\n### About Local Mode and the Domain of the DNS Server Itself\nThere are two scenarios for DNS requests sent by the DNS module:\n---\n**Local Mode** connections are made directly outwards by the core. In this case, if the address is a domain name, it will be resolved by the system itself. The logic is relatively simple.\n---\n**Non-Local** modes will essentially be treated as requests coming from an inbound with the tag `dns.tag` (Don't know where it is? Ctrl+F in your browser to search for `inboundTag`). They will go through the normal core processing flow and may be assigned by the routing module to a local freedom or other remote outbounds. They will be resolved by the freedom's `domainStrategy` (beware of potential loops) or sent directly as domains to the remote end to be resolved according to the server's own resolution method.\n---\nSince it might be difficult for average users to clarify the logic involved, it is recommended (especially in a transparent proxy environment) to **directly set the corresponding IPs for servers with domain names in the host option of the DNS module** to prevent loops.\n---\nIncidentally, DNS requests sent by the DNS module in non-local modes will automatically skip the `IPIfNonMatch` and `IPOnDemand` resolution processes in the routing module. This prevents their resolution from being sent back to the DNS module, causing a loop.\n"
              },
              "port": {
                "markdownDescription": "DNS server port, e.g., `53`. Defaults to `53` if omitted. This item is invalid when using DOH, DOHL, or DOQL modes; non-standard ports should be specified in the URL.\n",
                "allOf": [{ "$ref": "#/definitions/__schema21" }]
              },
              "domains": {
                "markdownDescription": "A list of domains. Domains included in this list will prioritize using this server for queries. The domain format is the same as in [Routing Configuration](https://xtls.github.io/en/config/routing.html#ruleobject).\n",
                "allOf": [{ "$ref": "#/definitions/__schema22" }]
              },
              "expectedIPs": {
                "markdownDescription": "A list of IP ranges. The format is the same as in [Routing Configuration](https://xtls.github.io/en/config/routing.html#ruleobject).\n\nWhen configured, Xray DNS will verify the returned IP and only return addresses included in the `expectedIPs` list.\n\nIf `*` exists in the list, and if no IP exists after filtering, the original IP is still returned so that the request does not fail.\n",
                "allOf": [{ "$ref": "#/definitions/__schema23" }]
              },
              "unexpectedIPs": {
                "markdownDescription": "The reverse version of `expectedIPs`. IPs included in this list are removed. The asterisk works the same way.\n",
                "allOf": [{ "$ref": "#/definitions/__schema24" }]
              },
              "skipFallback": {
                "markdownDescription": "`true`: Skip this server during DNS fallback queries. Defaults to `false` (not skipped).\n",
                "allOf": [{ "$ref": "#/definitions/__schema25" }]
              },
              "finalQuery": {
                "markdownDescription": "If set to true, the request to this DNS server will be the final attempt and will not trigger fallback behavior.\n",
                "allOf": [{ "$ref": "#/definitions/__schema26" }]
              },
              "tag": { "type": "string" },
              "clientIP": { "type": "string" },
              "queryStrategy": {
                "markdownDescription": "If not specified, it inherits from the global configuration; if specified, it allows further limiting the capabilities of this server and setting the default value for IP query types initiated by Xray itself.\n\nNote: It is always constrained by the global `queryStrategy`.\n\n### The following configuration items, if not specified, will inherit from the global configuration, or can override the global configuration here\n",
                "allOf": [{ "$ref": "#/definitions/__schema27" }]
              },
              "disableCache": { "default": false, "type": "boolean" }
            },
            "required": ["address"],
            "additionalProperties": false
          }
        ]
      }
    },
    "__schema21": { "type": "number" },
    "__schema22": { "type": "array", "items": { "type": "string" } },
    "__schema23": { "type": "array", "items": { "type": "string" } },
    "__schema24": { "type": "array", "items": { "type": "string" } },
    "__schema25": { "default": false, "type": "boolean" },
    "__schema26": { "default": false, "type": "boolean" },
    "__schema27": {
      "default": "UseIP",
      "type": "string",
      "enum": ["UseIP", "UseIPv4", "UseIPv6", "UseSystem"]
    },
    "__schema28": {
      "markdownDescription": "For query traffic generated by the built-in DNS, except for localhost, fakedns, TCPL, DOHL, and DOQL modes, this tag can be used in routing for matching via inboundTag.\n\n",
      "allOf": [{ "$ref": "#/definitions/__schema29" }]
    },
    "__schema29": { "type": "string" },
    "__schema30": {
      "markdownDescription": "The IP address used in the EDNS Client Subnet extension.\n---\nMust be a valid IPv4 or IPv6 address. When actually sent, the last few bits will be automatically masked; IPv4 and IPv6 are sent with /24 and /96 subnets respectively.\n",
      "allOf": [{ "$ref": "#/definitions/__schema31" }]
    },
    "__schema31": { "type": "string" },
    "__schema32": {
      "markdownDescription": "Limits the capabilities of all servers in the DNS module and sets the default value for IP query types initiated by Xray itself.\n---\nThe default value `UseIP` allows querying both A + AAAA records. When a query initiated by Xray itself does not specify an IP type, both A and AAAA records are queried from the upstream DNS server. `UseIPv4` only queries and allows querying A records; `UseIPv6` only queries and allows querying AAAA records.\n---\n`UseSystem` adapts to the operating system's network environment. Before querying, it checks whether there are IPv4 and IPv6 default gateways, thereby limiting the capabilities of all servers and setting the default query type. It checks in real-time on graphical OS environments and only once on command-line environments.\n---\n```json\n    \"dns\": {\n        \"servers\": [\n            \"https://1.1.1.1/dns-query\",\n            {\n                \"address\": \"https://8.8.8.8/dns-query\",\n                \"domains\": [\n                    \"geosite:netflix\"\n                ],\n                \"skipFallback\": true,\n                \"queryStrategy\": \"UseIPv4\" // netflix domain queries A record\n            },\n            {\n                \"address\": \"https://1.1.1.1/dns-query\",\n                \"domains\": [\n                    \"geosite:openai\"\n                ],\n                \"skipFallback\": true,\n                \"queryStrategy\": \"UseIPv6\" // openai domain queries AAAA record\n            }\n        ],\n        \"queryStrategy\": \"UseIP\" // Globally query both A and AAAA records\n    }\n```\n---\n### TIP 1\nThe global `\"queryStrategy\"` value takes precedence. When the `\"queryStrategy\"` value in a sub-item conflicts with the global `\"queryStrategy\"` value, the query for that sub-item will return an empty response.\n---\n### TIP 2\nWhen the `\"queryStrategy\"` parameter is not written in a sub-item, the global `\"queryStrategy\"` parameter value is used. This behavior is the same as versions prior to Xray-core v1.8.6.\n---\nFor example:<br>\nGlobal `\"queryStrategy\": \"UseIPv6\"` conflicts with sub-item `\"queryStrategy\": \"UseIPv4\"`.<br>\nGlobal `\"queryStrategy\": \"UseIPv4\"` conflicts with sub-item `\"queryStrategy\": \"UseIPv6\"`.<br>\nGlobal `\"queryStrategy\": \"UseIP\"` does not conflict with sub-item `\"queryStrategy\": \"UseIPv6\"`.<br>\nGlobal `\"queryStrategy\": \"UseIP\"` does not conflict with sub-item `\"queryStrategy\": \"UseIPv4\"`.\n---\n```json\n    \"dns\": {\n        \"servers\": [\n            \"https://1.1.1.1/dns-query\",\n            {\n                \"address\": \"https://8.8.8.8/dns-query\",\n                \"domains\": [\n                    \"geosite:netflix\"\n                ],\n                \"skipFallback\": true,\n                \"queryStrategy\": \"UseIPv6\" // Global \"UseIPv4\" conflicts with sub-item \"UseIPv6\"\n            }\n        ],\n        \"queryStrategy\": \"UseIPv4\"\n    }\n```\n---\nThe sub-item query for the Netflix domain returns an empty response due to the conflicting `\"queryStrategy\"` value. The Netflix domain is then queried by `https://1.1.1.1/dns-query`, returning an A record.\n",
      "allOf": [{ "$ref": "#/definitions/__schema33" }]
    },
    "__schema33": {
      "default": "UseIP",
      "type": "string",
      "enum": ["UseIP", "UseIPv4", "UseIPv6", "UseSystem"]
    },
    "__schema34": {
      "markdownDescription": "`true` disables DNS caching. Defaults to `false` (not disabled).\n\nThis does not affect `localhost` DNS (system DNS), which always follows Golang's DNS caching behavior (cgo and pure go may differ slightly).\n",
      "allOf": [{ "$ref": "#/definitions/__schema35" }]
    },
    "__schema35": { "default": false, "type": "boolean" },
    "__schema36": {
      "markdownDescription": "`true` disables DNS fallback queries. Defaults to `false` (not disabled).\n",
      "allOf": [{ "$ref": "#/definitions/__schema37" }]
    },
    "__schema37": { "default": false, "type": "boolean" },
    "__schema38": {
      "markdownDescription": "`true` disables fallback queries when the DNS server's priority domain list is matched. Defaults to `false` (not disabled).\n",
      "allOf": [{ "$ref": "#/definitions/__schema39" }]
    },
    "__schema39": { "default": false, "type": "boolean" },
    "__schema40": {
      "markdownDescription": "If true, appends the system hosts file to the built-in DNS hosts.\n\n",
      "allOf": [{ "$ref": "#/definitions/__schema41" }]
    },
    "__schema41": { "default": false, "type": "boolean" },
    "__schema42": {
      "markdownDescription": "`true` enables parallel queries. Defaults to `false` (not enabled).\n\nDNS failover is serial by default, meaning a query is sent to the next server only after the selected DNS server fails or `expectedIPs` and `unexpectedIPs` do not match.\n\nWhen parallel query is enabled, queries are initiated asynchronously to all selected DNS servers in advance, executing a strategy of \"dynamic grouping, intra-group racing, and inter-group fallback\".\n\n**Dynamic Grouping**: Adjacent servers in the selected server list are considered the same group if their `clientIP`, `skipFallback`, `queryStrategy`, `tag`, `domains`, `expectedIPs`, and `unexpectedIPs` are **exactly** the same.\n\n**Intra-group Racing**: If any DNS server in the same group queries successfully and the IP matches `expectedIPs` and `unexpectedIPs`, the group is considered successful, and results from other servers in the group are ignored.\n\n**Inter-group Fallback**: If the first group is still querying, wait. If the first group succeeds, return the IP. If all servers in the first group fail or IPs do not match, fallback to the next group. Finally, if all groups fail, return an empty resolution.\n\n> `useSystemHosts`: true | false\n\nIf true, appends the system hosts file to the built-in DNS hosts.\n\n> `tag`: string\n\nFor query traffic generated by the built-in DNS, except for `localhost`, `fakedns`, `TCPL`, `DOHL`, and `DOQL` modes, this tag can be used in routing for matching via `inboundTag`.\n\n### DnsServerObject\n\n```json\n{\n  \"address\": \"1.2.3.4\",\n  \"port\": 5353,\n  \"domains\": [\"domain:xray.com\"],\n  \"expectedIPs\": [\"geoip:cn\"],\n  \"unexpectedIPs\": [\"geoip:cloudflare\"],\n  \"skipFallback\": false,\n  \"finalQuery\": false,\n  \"tag\": \"dns-tag\",\n  \"clientIP\": \"1.2.3.4\",\n  \"queryStrategy\": \"UseIPv4\",\n  \"disableCache\": false\n}\n```\n",
      "allOf": [{ "$ref": "#/definitions/__schema43" }]
    },
    "__schema43": { "default": false, "type": "boolean" },
    "__schema44": {
      "markdownDescription": "`true` enables DNS optimistic caching. Defaults to `false` (not enabled).\n\nOnly effective when the server has DNS caching enabled (i.e., this option is constrained by `disableCache`).\n",
      "allOf": [{ "$ref": "#/definitions/__schema45" }]
    },
    "__schema45": { "default": false, "type": "boolean" },
    "__schema46": {
      "markdownDescription": "Validity period for optimistic caching in seconds. Defaults to 0, meaning it never expires.\n\nIf the server has caching enabled and optimistic caching is turned on: when the cache has expired but the optimistic cache has not, the stale DNS record in the cache is returned immediately, and the cache is refreshed in the background. This can reduce latency.\n",
      "allOf": [{ "$ref": "#/definitions/__schema47" }]
    },
    "__schema47": { "default": 0, "type": "number" },
    "__schema48": {},
    "__schema49": {
      "markdownDescription": "Domain resolution strategy. Different strategies are used based on different settings.\n\n- `\"AsIs\"`: No extra operation. Uses the domain in the destination address or the sniffed domain. Default value.\n- `\"IPIfNonMatch\"`: When no rule is matched after a full round of matching, resolve the domain to an IP and perform a second round of matching.\n- `\"IPOnDemand\"`: Before starting matching, resolve the domain to an IP immediately for matching.\n\nActual resolution behavior will be delayed until the first IP rule is encountered to reduce latency. The result will contain both IPv4 and IPv6 (you can further restrict this via `queryStrategy` in the built-in DNS). When a domain resolves to multiple IPs, each rule will try all IPs in turn. If any IP meets the requirement, the rule is considered matched.\n\nWhen `sniff` + `routeOnly` is enabled, allowing the routing system to see both IP and domain, if the aforementioned resolution occurs, the routing system can only see the IP resolved from the domain and cannot see the original destination IP, unless resolution fails.\n\nWhen two domains exist (target domain + sniffed result), the priority of the sniffed result is always higher, whether for resolution or domain matching.\n\nRegardless of whether resolution occurs, the routing system will not affect the actual destination address. The requested target remains the original target.\n",
      "allOf": [{ "$ref": "#/definitions/__schema50" }]
    },
    "__schema50": {
      "anyOf": [
        {
          "type": "string",
          "const": "AsIs",
          "markdownDescription": "No extra operation. Uses the domain in the destination address or the sniffed domain. Default value."
        },
        {
          "type": "string",
          "const": "IPIfNonMatch",
          "markdownDescription": "When no rule is matched after a full round of matching, resolve the domain to an IP and perform a second round of matching."
        },
        {
          "type": "string",
          "const": "IPOnDemand",
          "markdownDescription": "Before starting matching, resolve the domain to an IP immediately for matching."
        }
      ]
    },
    "__schema51": {
      "markdownDescription": "Corresponds to an array, where each item is a rule.\n---\nFor each connection, routing will judge these rules from top to bottom. When the first effective rule is encountered, the connection is forwarded to the `outboundTag` or `balancerTag` specified by it.\n---\nWhen no rule is matched, traffic is sent via the first outbound by default.\n",
      "allOf": [{ "$ref": "#/definitions/__schema52" }]
    },
    "__schema52": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "domain": {
            "markdownDescription": "- **Pure string**: Same as substring below, but the `\"keyword:\"` prefix can be omitted.\n---\n- **Regular expression**: Starts with `\"regexp:\"`, the rest is a regular expression. The rule takes effect when the regular expression matches the target domain. For example, \"regexp:\\\\.goo.*\\\\.com$\" matches \"www.google.com\" and \"fonts.googleapis.com\", but not \"google.com\". Case sensitive.\n---\n- **Subdomain (Recommended)**: Starts with `\"domain:\"`, the rest is a domain name. The rule takes effect when the domain is the target domain or its subdomain. For example, \"domain:xray.com\" matches \"www.xray.com\" and \"xray.com\", but not \"wxray.com\".\n---\n- **Substring**: Starts with `\"keyword:\"`, the rest is a string. The rule takes effect when this string matches any part of the target domain. For example, \"keyword:sina.com\" matches \"sina.com\", \"sina.com.cn\", and \"www.sina.com\", but not \"sina.cn\".\n---\n- **Full match**: Starts with `\"full:\"`, the rest is a domain name. The rule takes effect when this domain exactly matches the target domain. For example, \"full:xray.com\" matches \"xray.com\" but not \"www.xray.com\".\n---\n- **Dotless domain**: Starts with `\"dotless:\"`, the rest is a string that cannot contain .. The rule takes effect when the domain contains no `.` and this string matches any part of the target domain. For example, \"dotless:pc-\" matches \"pc-alice\", \"mypc-alice\". Suitable for intranet NetBIOS domains, etc. Case sensitive.\n---\n- **Predefined domain list**: Starts with `\"geosite:\"`, the rest is a name, such as `geosite:google` or `geosite:cn`. Refer to Predefined Domain List for names and domain lists.\n---\n- **Load domains from file**: In the form of `\"ext:file:tag\"`. Must start with `ext:` (lowercase), followed by filename and tag. The file is stored in the [Resource Directory](https://xtls.github.io/en/config/features/env.html#resource-file-path). The file format is the same as `geosite.dat`, and the tag must exist in the file.\n---\n- `\"ext:geoip.dat:cn\"` is equivalent to `\"geoip:cn\"`\n",
            "allOf": [{ "$ref": "#/definitions/__schema53" }]
          },
          "ip": {
            "markdownDescription": "An array, where each item represents an IP range. The rule takes effect when an item matches the target IP. Available forms:\n\n- **IP**: Like `\"127.0.0.1\"`.\n- **[CIDR](https://en.wikipedia.org/wiki/Classless_Inter-Domain_Routing)**: Like `\"10.0.0.0/8\"`. You can also use `\"0.0.0.0/0\"` or `\"::/0\"` to specify all IPv4 or IPv6.\n- **Predefined IP list**: This list is pre-installed in every Xray installation package, named `geoip.dat`. Usage is like `\"geoip:cn\"`. Must start with `geoip:` (lowercase), followed by a two-character country code. Supports almost all countries with internet access.\n  - **Special value**: `\"geoip:private\"`, includes all private addresses, such as `127.0.0.1`.\n- **Load IPs from file**: In the form of `\"ext:file:tag\"`. Must start with `ext:` (lowercase), followed by filename and tag. The file is stored in the [Resource Directory](https://xtls.github.io/en/config/features/env.html#resource-file-path). The file format is the same as `geoip.dat`, and the tag must exist in the file.\n- **Inverse selection `!`**: `\"!10.0.0.0/8\"` means anything not in `10.0.0.0/8`, and `\"!geoip:cn\"` means results not in `geoip:cn`. Multiple inverse options have an `AND` relationship, while positive options, or positive options together with all inverse options, have an `OR` relationship. For example, `ip: [\"!geoip:cn\", \"!geoip:us\", \"geoip:telegram\"]` matches IPs that are not from the US AND not from China, OR are Telegram IPs.\n",
            "allOf": [{ "$ref": "#/definitions/__schema54" }]
          },
          "port": {
            "markdownDescription": "Target port range. Three forms:\n\n- `\"a-b\"`: a and b are positive integers less than 65536. This is a closed interval. The rule takes effect when the target port falls within this range.\n- `a`: a is a positive integer less than 65536. The rule takes effect when the target port is a.\n- A mixture of the above two forms, separated by commas \",\". For example: `\"53,443,1000-2000\"`.\n",
            "allOf": [{ "$ref": "#/definitions/__schema55" }]
          },
          "sourcePort": {
            "markdownDescription": "Source port. Three forms:\n\n- `\"a-b\"`: a and b are positive integers less than 65536. This is a closed interval. The rule takes effect when the source port falls within this range.\n- `a`: a is a positive integer less than 65536. The rule takes effect when the source port is a.\n- A mixture of the above two forms, separated by commas \",\". For example: `\"53,443,1000-2000\"`.\n",
            "allOf": [{ "$ref": "#/definitions/__schema56" }]
          },
          "network": {
            "markdownDescription": "Optional values are \"tcp\", \"udp\", or \"tcp,udp\". The rule takes effect when the connection method matches.\n\nSince the core obviously only supports TCP and UDP layer 4 protocols, a routing rule containing only `\"network\": \"tcp,udp\"` can be used as a \"catch-all\" to match any traffic. An example usage is placing it at the very end of all routing rules to specify the default outbound when no other rules match (otherwise the core defaults to the first outbound).\n\nOf course, other ways that obviously match any traffic, such as specifying ports 1-65535 or IPs 0.0.0.0/0 + ::/0, have a similar effect.\n",
            "allOf": [{ "$ref": "#/definitions/__schema57" }]
          },
          "source": {
            "markdownDescription": "An array, where each item represents an IP range. Forms include IP, CIDR, GeoIP, and loading IPs from a file. The rule takes effect when an item matches the source IP.\n\n---\n\nAlias: `source`\n",
            "allOf": [{ "$ref": "#/definitions/__schema58" }]
          },
          "sourceIP": {
            "markdownDescription": "An array, where each item represents an IP range. Forms include IP, CIDR, GeoIP, and loading IPs from a file. The rule takes effect when an item matches the source IP.\n\n---\n\nAlias: `source`\n",
            "allOf": [{ "$ref": "#/definitions/__schema59" }]
          },
          "user": {
            "markdownDescription": "An array, where each item is an email address. The rule takes effect when an item matches the source user.\n\nSimilar to domains, it also supports regex matching starting with `regexp:`. (Similarly, need to replace `\\` with `\\\\`, see explanation in domain section).\n",
            "allOf": [{ "$ref": "#/definitions/__schema60" }]
          },
          "vlessRoute": {
            "markdownDescription": "VLESS inbound allows the client to modify the 7th and 8th bytes of the configured UUID to any bytes. The server routing will use this as `vlessRoute` data, allowing users to customize parts of the server routing based on needs without changing any external fields.\n\n```\n--------------↓↓↓↓------------------\nxxxxxxxx-xxxx-0000-xxxx-xxxxxxxxxxxx\n```\n\nThe configuration uses data after Big-Endian encoding to uint16 (if you don't understand, treat these four digits as a hexadecimal number and convert to decimal). E.g., `0001→1`, `000e→14`, `38b2→14514`. The reason for this is that the syntax here is the same as `port`; you can freely specify many segments for routing just like specifying ports.\n",
            "allOf": [{ "$ref": "#/definitions/__schema61" }]
          },
          "inboundTag": {
            "markdownDescription": "An array, where each item is an identifier. The rule takes effect when an item matches the identifier of the inbound protocol.\n",
            "allOf": [{ "$ref": "#/definitions/__schema62" }]
          },
          "protocol": {
            "markdownDescription": "An array, where each item represents a protocol. The rule takes effect when a protocol matches the protocol type of the current connection.\n\n- `http` only supports 1.0 and 1.1; h2 is not supported yet (plaintext h2 traffic is also very rare).\n---\n- `tls` TLS 1.0 ~ 1.3.\n---\n- `quic` Due to the complexity of this protocol, sniffing may sometimes fail.\n---\n- `bittorrent` Only has the most basic sniffing; may not work for much encrypted and obfuscated traffic.\n---\n\nYou must enable the `sniffing` option in the inbound proxy to sniff the protocol type used by the connection.\n",
            "allOf": [{ "$ref": "#/definitions/__schema63" }]
          },
          "attrs": {
            "markdownDescription": "A JSON object where keys and values are strings. Used to detect HTTP traffic attribute values (due to obvious reasons, only supports 1.0 and 1.1). The rule is matched when HTTP headers contain all specified keys and values contain the specified substrings. Keys are case-insensitive. Values support regular expressions.\n\nIt also supports pseudo-headers like `:method` and `:path` from h2 for matching methods and paths (although these headers do not exist in HTTP/1.1).\n\nFor non-CONNECT methods of HTTP inbounds, `attrs` can be obtained directly. For other inbounds, sniffing must be enabled to obtain these values for matching.\n\nExample:\n\n- Detect HTTP GET: `{\":method\": \"GET\"}`\n- Detect HTTP Path: `{\":path\": \"/test\"}`\n- Detect Content Type: `{\"accept\": \"text/html\"}`\n",
            "allOf": [{ "$ref": "#/definitions/__schema64" }]
          },
          "process": {
            "markdownDescription": "If the connection originates from the local machine, match its process. If not from local, it is directly regarded as a match failure. Only supports Windows and Linux.\n\nThis option is an array, where each item has three matching modes.\n\n1. **No slash**: Matches process name.\n2. **Contains slash, does not end with slash**: Matches absolute path.\n3. **Contains slash, ends with slash**: Matches folder; all processes under this folder are considered a match.\n\nNote:\n\n- All options are case-sensitive.\n- On Windows, use backslash `\\` for paths. Here it is uniformly required to use forward slash `/`, e.g., `C:/Windows/System32/curl.exe`, because backslashes are treated as escape characters in JSON, which is inconvenient (unless you choose to double the backslashes, which also works).\n- When matching by process name, the core automatically removes the `.exe` suffix. Similarly, `[\"curl\"]` can match curl on both Linux and Windows. When using absolute paths, the `.exe` suffix cannot be ignored.\n\nSpecial syntax sugar:\n\n- `self/`: Matches the current core process, very useful for avoiding routing loops.\n- `xray/`: Will be replaced by the absolute path where the current core resides, matching all Xray processes started from this binary.\n",
            "allOf": [{ "$ref": "#/definitions/__schema65" }]
          },
          "outboundTag": {
            "markdownDescription": "Corresponds to an outbound identifier.\n",
            "allOf": [{ "$ref": "#/definitions/__schema66" }]
          },
          "balancerTag": {
            "markdownDescription": "Corresponds to a Balancer identifier.\n---\nYou must choose one between `balancerTag` and `outboundTag`. When both are specified, `outboundTag` takes effect.\n\n",
            "allOf": [{ "$ref": "#/definitions/__schema67" }]
          },
          "ruleTag": {
            "markdownDescription": "Optional. No actual effect, only used to identify the name of this rule.\n\nIf set, information regarding this rule will be output at the Info level when the rule is matched, used for debugging which specific rule was hit.\n",
            "allOf": [{ "$ref": "#/definitions/__schema68" }]
          },
          "webhook": {
            "type": "object",
            "properties": {
              "url": {
                "type": "string",
                "markdownDescription": "The URL to which the notification will be sent. Both standard web addresses and local Unix socket paths are supported.\n\n- `https://api.example.com/alert` — a standard URL for sending notifications via HTTP(S). Used for integration with external web services or APIs.\n- `/var/run/webhook.sock` — sends the notification via a Unix socket, with the POST request directed to the root path `/` of the socket.\n- `/var/run/webhook.sock:/alert` — sends the notification via a Unix socket to a specific endpoint `/alert`. This allows direct integration with local services without using a network interface.\n- `@abstract:/webhook` — abstract socket (lock-free, Linux/Android only).\n- `@@padded:/webhook` — abstract socket with padding for HAProxy compatibility.\n"
              },
              "deduplication": {
                "markdownDescription": "The time (in seconds) for event deduplication. If multiple events are triggered within this period, duplicate requests are ignored.\n",
                "allOf": [{ "$ref": "#/definitions/__schema69" }]
              },
              "headers": {
                "type": "object",
                "properties": {},
                "additionalProperties": { "$ref": "#/definitions/__schema70" },
                "markdownDescription": "HTTP request headers."
              }
            },
            "required": ["url", "headers"],
            "additionalProperties": false
          }
        },
        "additionalProperties": {}
      }
    },
    "__schema53": { "type": "array", "items": { "type": "string" } },
    "__schema54": { "type": "array", "items": { "type": "string" } },
    "__schema55": { "anyOf": [{ "type": "string" }, { "type": "number" }] },
    "__schema56": { "anyOf": [{ "type": "string" }, { "type": "number" }] },
    "__schema57": { "type": "string", "enum": ["tcp", "udp", "tcp,udp"] },
    "__schema58": { "type": "array", "items": { "type": "string" } },
    "__schema59": { "type": "array", "items": { "type": "string" } },
    "__schema60": { "type": "array", "items": { "type": "string" } },
    "__schema61": { "type": "string" },
    "__schema62": { "type": "array", "items": { "type": "string" } },
    "__schema63": { "type": "array", "items": { "type": "string" } },
    "__schema64": {
      "type": "object",
      "propertyNames": { "type": "string" },
      "additionalProperties": {}
    },
    "__schema65": { "type": "array", "items": { "type": "string" } },
    "__schema66": { "type": "string" },
    "__schema67": { "type": "string" },
    "__schema68": { "type": "string" },
    "__schema69": { "type": "number" },
    "__schema70": {},
    "__schema71": {
      "markdownDescription": "An array, where each item is a load balancer configuration.\n\nWhen a rule points to a load balancer, Xray will select an outbound through this load balancer and then forward traffic using it.\n",
      "allOf": [{ "$ref": "#/definitions/__schema72" }]
    },
    "__schema72": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "tag": {
            "markdownDescription": "The identifier of this load balancer, used to match `balancerTag` in `RuleObject`.\n",
            "allOf": [{ "$ref": "#/definitions/__schema73" }]
          },
          "selector": {
            "markdownDescription": "An array of strings. Each string is used for prefix matching against outbound identifiers. Among the following outbound identifiers: `[ \"a\", \"ab\", \"c\", \"ba\" ]`, `\"selector\": [\"a\"]` will match `[ \"a\", \"ab\" ]`.\n\nGenerally matches multiple outbounds to distribute load among them.\n",
            "allOf": [{ "$ref": "#/definitions/__schema74" }]
          },
          "fallbackTag": {
            "markdownDescription": "If all outbounds cannot be connected based on observation results, the outbound specified by this configuration item is used.\n",
            "allOf": [{ "$ref": "#/definitions/__schema75" }]
          },
          "strategy": {
            "oneOf": [
              {
                "type": "object",
                "properties": {
                  "type": {
                    "type": "string",
                    "const": "random",
                    "markdownDescription": "Default value. Randomly selects a matched outbound proxy.\n\nThis can optionally use an observatory. If `fallbackTag` is set and an observatory is present, outbounds observed as unavailable will be automatically excluded (those without observation data are assumed to be alive).\n"
                  }
                },
                "required": ["type"],
                "additionalProperties": false
              },
              {
                "type": "object",
                "properties": {
                  "type": {
                    "type": "string",
                    "const": "roundRobin",
                    "markdownDescription": "Selects matched outbound proxies in order.\n\nThis can optionally use an observatory. If `fallbackTag` is set and an observatory is present, outbounds observed as unavailable will be automatically excluded (those without observation data are assumed to be alive).\n"
                  }
                },
                "required": ["type"],
                "additionalProperties": false
              },
              {
                "type": "object",
                "properties": {
                  "type": {
                    "type": "string",
                    "const": "leastPing",
                    "markdownDescription": "Selects the matched outbound proxy with the lowest latency based on observation results.\n\nThis strategy must be used with an observatory, and nodes not covered by the observatory will be directly excluded. If all are unavailable and `fallbackTag` is not set, the default outbound will be selected.\n\n"
                  }
                },
                "required": ["type"],
                "additionalProperties": false
              },
              {
                "type": "object",
                "properties": {
                  "type": {
                    "type": "string",
                    "const": "leastLoad",
                    "markdownDescription": "Selects the most stable outbound proxy based on observation results.\n\nThis strategy must be used with an observatory, and nodes not covered by the observatory will be directly excluded. If all are unavailable and `fallbackTag` is not set, the default outbound will be selected.\n"
                  },
                  "settings": {
                    "type": "object",
                    "properties": {
                      "expected": { "$ref": "#/definitions/__schema76" },
                      "maxRTT": { "$ref": "#/definitions/__schema78" },
                      "tolerance": { "$ref": "#/definitions/__schema80" },
                      "baselines": { "$ref": "#/definitions/__schema82" },
                      "costs": { "$ref": "#/definitions/__schema84" }
                    },
                    "additionalProperties": false,
                    "markdownDescription": "The configuration format varies for different load balancing strategies. Currently, only the leastLoad strategy supports this item."
                  }
                },
                "required": ["type"],
                "additionalProperties": false
              }
            ]
          }
        },
        "required": ["strategy"],
        "additionalProperties": false
      }
    },
    "__schema73": { "type": "string" },
    "__schema74": { "type": "array", "items": { "type": "string" } },
    "__schema75": { "type": "string" },
    "__schema76": {
      "markdownDescription": "The number of optimal nodes selected by the load balancer. Traffic will be randomly distributed among these nodes.",
      "allOf": [{ "$ref": "#/definitions/__schema77" }]
    },
    "__schema77": { "type": "number" },
    "__schema78": {
      "markdownDescription": "The maximum acceptable RTT duration for speed tests.",
      "allOf": [{ "$ref": "#/definitions/__schema79" }]
    },
    "__schema79": { "type": "string" },
    "__schema80": {
      "markdownDescription": "The maximum acceptable failure rate for speed tests. For example, 0.01 means accepting a 1% failure rate. (Seemingly unimplemented).",
      "allOf": [{ "$ref": "#/definitions/__schema81" }]
    },
    "__schema81": {
      "type": "number",
      "minimum": -3.4028234663852886e38,
      "maximum": 3.4028234663852886e38
    },
    "__schema82": {
      "markdownDescription": "The maximum acceptable standard deviation duration for RTT speed tests.",
      "allOf": [{ "$ref": "#/definitions/__schema83" }]
    },
    "__schema83": { "type": "array", "items": { "type": "string" } },
    "__schema84": {
      "markdownDescription": "Optional configuration item. An array to assign weights to all outbounds.",
      "allOf": [{ "$ref": "#/definitions/__schema85" }]
    },
    "__schema85": {
      "default": [],
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "regexp": {
            "markdownDescription": "Whether to use regular expressions to select outbound `Tag`.",
            "allOf": [{ "$ref": "#/definitions/__schema86" }]
          },
          "match": {
            "markdownDescription": "Matches outbound `Tag`.",
            "allOf": [{ "$ref": "#/definitions/__schema87" }]
          },
          "value": {
            "markdownDescription": "Weight value. The larger the value, the less likely the corresponding node is to be selected.",
            "allOf": [{ "$ref": "#/definitions/__schema88" }]
          }
        },
        "additionalProperties": false
      }
    },
    "__schema86": { "default": false, "type": "boolean" },
    "__schema87": { "type": "string" },
    "__schema88": {
      "type": "number",
      "minimum": -3.4028234663852886e38,
      "maximum": 3.4028234663852886e38
    },
    "__schema89": {},
    "__schema90": {
      "markdownDescription": "A set of key-value pairs, where each key is a number in string format (required by JSON), such as `\"0\"`, `\"1\"`, etc. The double quotes cannot be omitted. This number corresponds to the user level. Each value is a [LevelPolicyObject](https://xtls.github.io/en/config/policy.html#levelpolicyobject).",
      "allOf": [{ "$ref": "#/definitions/__schema91" }]
    },
    "__schema91": {
      "type": "object",
      "propertyNames": { "type": "string" },
      "additionalProperties": {
        "type": "object",
        "properties": {
          "handshake": {
            "markdownDescription": "Handshake time limit when establishing a connection. Unit is seconds. Default value is `4`. When an inbound proxy processes a new connection, if the time used during the handshake phase exceeds this time, the connection is interrupted.",
            "allOf": [{ "$ref": "#/definitions/__schema92" }]
          },
          "connIdle": {
            "markdownDescription": "Connection idle time limit. Unit is seconds. Default value is `300`. When an inbound/outbound processes a connection, if no data is transferred (including uplink and downlink data) within the `connIdle` time, the connection is interrupted.",
            "allOf": [{ "$ref": "#/definitions/__schema93" }]
          },
          "uplinkOnly": {
            "markdownDescription": "Time limit after the downlink connection is closed. Unit is seconds. Default value is `2`. When the server (such as a remote website) closes the downlink connection, the outbound proxy will interrupt the connection after waiting for the `uplinkOnly` time.",
            "allOf": [{ "$ref": "#/definitions/__schema94" }]
          },
          "downlinkOnly": {
            "markdownDescription": "Time limit after the uplink connection is closed. Unit is seconds. Default value is `5`. When the client (such as a browser) closes the uplink connection, the inbound proxy will interrupt the connection after waiting for the `downlinkOnly` time.\n---\nIn HTTP browsing scenarios, `uplinkOnly` and `downlinkOnly` can be set to 0 to improve connection closing efficiency.",
            "allOf": [{ "$ref": "#/definitions/__schema95" }]
          },
          "statsUserUplink": {
            "markdownDescription": "When set to `true`, enables uplink traffic statistics for all users of the current level.",
            "allOf": [{ "$ref": "#/definitions/__schema96" }]
          },
          "statsUserDownlink": {
            "markdownDescription": "When set to `true`, enables downlink traffic statistics for all users of the current level.",
            "allOf": [{ "$ref": "#/definitions/__schema97" }]
          },
          "statsUserOnline": {
            "markdownDescription": "When set to `true`, enables online user count statistics for all users of the current level. (Online criteria: connection activity within 20 seconds).",
            "allOf": [{ "$ref": "#/definitions/__schema98" }]
          },
          "bufferSize": {
            "markdownDescription": "The internal buffer size for each request, in KB. Note that multiple requests may be carried on the same connection via multiplexing (e.g., when using mux.cool or gRPC). This means that even if they share an underlying connection, their buffer pools are independent.\n---\nWhen the internal buffer is larger than this value, the next write operation will only be performed after the internal buffer is sent out until it is less than or equal to this value.\n---\nNote that for a UDP request, if a write attempt is made while the buffer is full, the write operation will not be blocked but discarded. If set too low or to 0, it may cause unexpected bandwidth waste.\n---\nDefault values:\n\n- On ARM, MIPS, MIPSLE platforms, the default value is `0`.\n- On ARM64, MIPS64, MIPS64LE platforms, the default value is `4`.\n- On other platforms, the default value is `512`.\n---\nThe default value can be set via the environment variable `XRAY_RAY_BUFFER_SIZE`. Note that the unit in the environment variable is MB (setting the environment variable to 1 is equivalent to setting the config to 1024).\n",
            "allOf": [{ "$ref": "#/definitions/__schema99" }]
          }
        },
        "additionalProperties": false
      }
    },
    "__schema92": { "default": 4, "type": "number" },
    "__schema93": { "default": 300, "type": "number" },
    "__schema94": { "default": 2, "type": "number" },
    "__schema95": { "default": 5, "type": "number" },
    "__schema96": { "default": false, "type": "boolean" },
    "__schema97": { "default": false, "type": "boolean" },
    "__schema98": { "default": false, "type": "boolean" },
    "__schema99": { "type": "number" },
    "__schema100": {
      "markdownDescription": "Xray system-level policies.",
      "allOf": [{ "$ref": "#/definitions/__schema101" }]
    },
    "__schema101": {
      "type": "object",
      "properties": {
        "statsInboundUplink": {
          "markdownDescription": "When set to `true`, enables uplink traffic statistics for all inbound proxies.",
          "allOf": [{ "$ref": "#/definitions/__schema102" }]
        },
        "statsInboundDownlink": {
          "markdownDescription": "When set to `true`, enables downlink traffic statistics for all inbound proxies.",
          "allOf": [{ "$ref": "#/definitions/__schema103" }]
        },
        "statsOutboundUplink": {
          "markdownDescription": "When set to `true`, enables uplink traffic statistics for all outbound proxies.",
          "allOf": [{ "$ref": "#/definitions/__schema104" }]
        },
        "statsOutboundDownlink": {
          "markdownDescription": "When set to `true`, enables downlink traffic statistics for all outbound proxies.",
          "allOf": [{ "$ref": "#/definitions/__schema105" }]
        }
      },
      "additionalProperties": false
    },
    "__schema102": { "default": false, "type": "boolean" },
    "__schema103": { "default": false, "type": "boolean" },
    "__schema104": { "default": false, "type": "boolean" },
    "__schema105": { "default": false, "type": "boolean" },
    "__schema106": {},
    "__schema107": {
      "type": "object",
      "properties": {
        "tag": {
          "type": "string",
          "markdownDescription": "The outbound proxy tag corresponding to metrics. You can access it by setting up a dokodemo-door inbound + routing rules that point the dokodemo-door to this outbound."
        },
        "listen": {
          "markdownDescription": "Directly listen on an address and port to provide the service.\n---\n[Usage ↗](https://xtls.github.io/en/config/metrics.html#usage)",
          "allOf": [{ "$ref": "#/definitions/__schema108" }]
        }
      },
      "required": ["tag"],
      "additionalProperties": false
    },
    "__schema108": { "type": "string" },
    "__schema109": {
      "type": "object",
      "properties": {
        "tag": {
          "markdownDescription": "The outbound proxy tag corresponding to metrics. You can access it by setting up a dokodemo-door inbound + routing rules that point the dokodemo-door to this outbound.",
          "allOf": [{ "$ref": "#/definitions/__schema110" }]
        },
        "listen": {
          "type": "string",
          "markdownDescription": "Directly listen on an address and port to provide the service.\n---\n[Usage ↗](https://xtls.github.io/en/config/metrics.html#usage)"
        }
      },
      "required": ["listen"],
      "additionalProperties": false
    },
    "__schema110": { "type": "string" },
    "__schema111": {
      "type": "array",
      "items": { "$ref": "#/definitions/__schema112" },
      "markdownDescription": "An array of strings, where each string is used for prefix matching against outbound proxy tags. Given the following outbound proxy tags: `[ \"a\", \"ab\", \"c\", \"ba\" ]`, `\"subjectSelector\": [\"a\"]` will match `[ \"a\", \"ab\" ]`."
    },
    "__schema112": { "type": "string" },
    "__schema113": {
      "default": "https://www.google.com/generate_204",
      "markdownDescription": "The URL used to probe the connection status of the outbound proxy.",
      "allOf": [{ "$ref": "#/definitions/__schema114" }]
    },
    "__schema114": { "type": "string" },
    "__schema115": {
      "type": "string",
      "markdownDescription": "The interval for initiating probes. The time format is number + unit, such as `\"10s\"`, `\"2h45m\"`. Supported time units are `ns`, `us`, `ms`, `s`, `m`, `h`, corresponding to nanoseconds, microseconds, milliseconds, seconds, minutes, and hours respectively.\n\nNote that since the request interval is fixed, periodic fixed requests might lead to behavioral fingerprinting. Using protocols with multiplexing or enabling `mux` can alleviate this issue."
    },
    "__schema116": {
      "markdownDescription": "- `true`: Probe all matched outbound proxies concurrently. Pauses for the time set in `probeInterval` after all are completed.\n- `false`: Probe matched outbound proxies one by one. Pauses for the time set in `probeInterval` after each outbound proxy is probed.\n",
      "allOf": [{ "$ref": "#/definitions/__schema117" }]
    },
    "__schema117": { "default": false, "type": "boolean" },
    "__schema118": {
      "type": "array",
      "items": { "$ref": "#/definitions/__schema119" },
      "markdownDescription": "An array of strings, where each string is used for prefix matching against outbound proxy tags. Given the following outbound proxy tags: `[ \"a\", \"ab\", \"c\", \"ba\" ]`, `\"subjectSelector\": [\"a\"]` will match `[ \"a\", \"ab\" ]`."
    },
    "__schema119": { "type": "string" },
    "__schema120": {
      "type": "object",
      "properties": {
        "destination": {
          "markdownDescription": "The URL used to probe the connection status of the outbound proxy. This URL should return an HTTP 204 success status code.",
          "allOf": [{ "$ref": "#/definitions/__schema121" }]
        },
        "connectivity": {
          "markdownDescription": "The URL used to check local network connectivity. This URL should return an HTTP 204 success status code.\n\nAn empty string indicates no local network connectivity check.\n\nThis probe is executed only when the `destination` probe fails. This makes the cause of network failure clearer in the logs.\n\nNote: In transparent proxy mode, this request might be captured by the transparent proxy and re-enter Xray for routing (depending on your configuration). You need to use extra means to ensure it is not captured by the transparent proxy, such as bypassing based on the URL IP, or using cgroup/pid routing to completely prevent Xray's requests from being captured. Alternatively, you can choose a URL that matches a direct connection rule and allow this request to be captured by the transparent proxy.\n",
          "allOf": [{ "$ref": "#/definitions/__schema122" }]
        },
        "interval": {
          "markdownDescription": "The expected **average** probe interval for each outbound proxy.\n\nThe time format is number + unit, such as `\"10s\"`, `\"2h45m\"`. Supported time units are `ns`, `us`, `ms`, `s`, `m`, `h`, corresponding to nanoseconds, microseconds, milliseconds, seconds, minutes, and hours respectively.\nMinimum allowed value is `\"10s\"`. If less is specified, `\"10s\"` will be used.\n",
          "allOf": [{ "$ref": "#/definitions/__schema123" }]
        },
        "sampling": {
          "markdownDescription": "The number of recent probe results to keep",
          "allOf": [{ "$ref": "#/definitions/__schema125" }]
        },
        "timeout": {
          "markdownDescription": "Probe timeout. Format is the same as interval above.\nThe time format is number + unit, such as `\"10s\"`, `\"2h45m\"`. Supported time units are `ns`, `us`, `ms`, `s`, `m`, `h`, corresponding to nanoseconds, microseconds, milliseconds, seconds, minutes, and hours respectively.",
          "allOf": [{ "$ref": "#/definitions/__schema126" }]
        },
        "httpMethod": {
          "markdownDescription": "The HTTP method used for probing (e.g. `\"HEAD\"`, `\"GET\"`)",
          "allOf": [{ "$ref": "#/definitions/__schema127" }]
        }
      },
      "additionalProperties": false
    },
    "__schema121": {
      "default": "https://connectivitycheck.gstatic.com/generate_204",
      "type": "string"
    },
    "__schema122": { "default": "", "type": "string" },
    "__schema123": { "default": "1m", "allOf": [{ "$ref": "#/definitions/__schema124" }] },
    "__schema124": { "type": "string", "pattern": "^-?\\d+(ns|us|ms|s|m|h)$" },
    "__schema125": { "default": 10, "type": "integer", "minimum": 0, "maximum": 9007199254740991 },
    "__schema126": { "default": "5s", "allOf": [{ "$ref": "#/definitions/__schema124" }] },
    "__schema127": {
      "default": "HEAD",
      "type": "string",
      "enum": ["GET", "POST", "HEAD", "DELETE", "PATCH", "PUT"]
    },
    "__schema128": {
      "type": "object",
      "properties": {
        "bridges": { "type": "array", "items": { "$ref": "#/definitions/__schema129" } },
        "portals": {
          "minItems": 1,
          "type": "array",
          "items": { "$ref": "#/definitions/__schema134" }
        }
      },
      "required": ["portals"],
      "additionalProperties": false
    },
    "__schema129": {
      "type": "object",
      "properties": {
        "tag": { "$ref": "#/definitions/__schema130" },
        "domain": { "$ref": "#/definitions/__schema132" }
      },
      "additionalProperties": false,
      "markdownDescription": "An array, where each item represents a `bridge`. The configuration for each `bridge` is a [BridgeObject](https://xtls.github.io/en/config/reverse.html#bridgeobject)."
    },
    "__schema130": {
      "markdownDescription": "All connections initiated by the `bridge` will carry this tag. It can be identified using `inboundTag` in the [Routing Configuration](https://xtls.github.io/en/config/routing.html).",
      "allOf": [{ "$ref": "#/definitions/__schema131" }]
    },
    "__schema131": { "type": "string" },
    "__schema132": {
      "markdownDescription": "Specifies a domain name. Connections established by the `bridge` to the `portal` will be sent using this domain.\nThis domain is used solely for communication between the `bridge` and the `portal` and does not need to exist in reality.",
      "allOf": [{ "$ref": "#/definitions/__schema133" }]
    },
    "__schema133": { "type": "string" },
    "__schema134": {
      "type": "object",
      "properties": {
        "tag": { "$ref": "#/definitions/__schema135" },
        "domain": { "$ref": "#/definitions/__schema137" }
      },
      "additionalProperties": false,
      "markdownDescription": "An array, where each item represents a `portal`. The configuration for each `portal` is a [PortalObject](https://xtls.github.io/en/config/reverse.html#bridgeobject)."
    },
    "__schema135": {
      "markdownDescription": "The identifier for the `portal`. Use `outboundTag` in the [Routing Configuration](https://xtls.github.io/en/config/routing.html) to forward traffic to this `portal`.",
      "allOf": [{ "$ref": "#/definitions/__schema136" }]
    },
    "__schema136": { "type": "string" },
    "__schema137": {
      "markdownDescription": "A domain name. When the `portal` receives traffic, if the target domain of the traffic matches this domain, the `portal` considers the current connection to be a communication connection sent by the `bridge`. Other traffic will be treated as traffic that needs to be forwarded. The job of the `portal` is to identify these two types of connections and perform the corresponding forwarding.\n\nA single Xray instance can act as a `bridge`, a `portal`, or both simultaneously to suit different scenario requirements.",
      "allOf": [{ "$ref": "#/definitions/__schema138" }]
    },
    "__schema138": { "type": "string" },
    "__schema139": {
      "type": "object",
      "properties": {
        "bridges": {
          "minItems": 1,
          "type": "array",
          "items": { "$ref": "#/definitions/__schema129" }
        },
        "portals": { "type": "array", "items": { "$ref": "#/definitions/__schema134" } }
      },
      "required": ["bridges"],
      "additionalProperties": false
    },
    "__schema140": {
      "oneOf": [
        { "$ref": "#/definitions/__schema141" },
        { "$ref": "#/definitions/__schema472" },
        { "$ref": "#/definitions/__schema476" },
        { "$ref": "#/definitions/__schema477" }
      ],
      "defaultSnippets": [
        {
          "label": "VLESS + Reality",
          "description": "Minimal VLESS inbound with Reality TLS",
          "body": {
            "protocol": "vless",
            "listen": "0.0.0.0",
            "port": 443,
            "tag": "VLESS-REALITY-SNIPPET",
            "settings": { "clients": [{ "id": "$1" }] },
            "streamSettings": { "network": "tcp", "security": "reality" },
            "sniffing": {
              "enabled": true,
              "routeOnly": true,
              "destOverride": ["http", "tls", "quic", "fakedns"]
            }
          }
        },
        {
          "label": "Wireguard",
          "description": "Minimal Wireguard inbound setup",
          "body": {
            "protocol": "wireguard",
            "listen": "0.0.0.0",
            "port": 52820,
            "tag": "WG-SNIPPET",
            "settings": {
              "secretKey": "$1",
              "mtu": 1420,
              "peers": [{ "allowedIPs": ["$2"], "publicKey": "$3" }]
            },
            "sniffing": {
              "enabled": true,
              "routeOnly": true,
              "destOverride": ["http", "tls", "quic", "fakedns"]
            }
          }
        }
      ]
    },
    "__schema141": {
      "type": "object",
      "properties": {
        "tag": { "$ref": "#/definitions/__schema142" },
        "listen": { "$ref": "#/definitions/__schema143" },
        "port": { "$ref": "#/definitions/__schema144" },
        "streamSettings": { "$ref": "#/definitions/__schema145" },
        "sniffing": { "$ref": "#/definitions/__schema471" },
        "protocol": { "type": "string", "const": "vless" },
        "settings": {
          "type": "object",
          "properties": {
            "decryption": {
              "default": "none",
              "anyOf": [{ "type": "string", "const": "none" }, { "type": "string" }]
            },
            "clients": {
              "default": [],
              "type": "array",
              "items": {
                "type": "object",
                "properties": {
                  "id": { "type": "string", "minLength": 1 },
                  "level": { "default": 0, "type": "number" },
                  "email": { "type": "string" },
                  "flow": { "default": "", "type": "string", "enum": ["", "xtls-rprx-vision"] },
                  "reverse": {
                    "default": {},
                    "type": "object",
                    "properties": { "tag": { "type": "string", "minLength": 1 } },
                    "additionalProperties": false
                  }
                },
                "required": ["id"],
                "additionalProperties": {}
              }
            },
            "fallbacks": {
              "type": "array",
              "items": {
                "type": "object",
                "properties": {
                  "name": { "default": "", "type": "string" },
                  "alpn": { "default": "", "type": "string" },
                  "path": { "default": "", "type": "string" },
                  "dest": { "anyOf": [{ "type": "string" }, { "type": "number" }] },
                  "xver": { "default": 0, "type": "number" }
                },
                "additionalProperties": false
              }
            }
          },
          "required": ["decryption"],
          "additionalProperties": false
        }
      },
      "required": ["port", "streamSettings", "protocol", "settings"],
      "additionalProperties": false
    },
    "__schema142": { "type": "string" },
    "__schema143": { "type": "string" },
    "__schema144": {
      "anyOf": [
        { "type": "integer", "minimum": 1, "maximum": 65535 },
        { "type": "string", "pattern": "^\\d+(?:-\\d+)?(?:,\\d+(?:-\\d+)?)*$" },
        { "type": "string", "pattern": "^env:[A-Za-z_][A-Za-z0-9_]*$" }
      ]
    },
    "__schema145": {
      "oneOf": [
        {
          "type": "object",
          "properties": {
            "network": { "$ref": "#/definitions/__schema146" },
            "rawSettings": { "$ref": "#/definitions/__schema147" },
            "security": { "$ref": "#/definitions/__schema169" },
            "realitySettings": { "$ref": "#/definitions/__schema170" },
            "finalmask": { "$ref": "#/definitions/__schema195" },
            "sockopt": { "$ref": "#/definitions/__schema296" }
          },
          "required": ["network", "security", "realitySettings"],
          "additionalProperties": false
        },
        {
          "type": "object",
          "properties": {
            "network": { "$ref": "#/definitions/__schema146" },
            "rawSettings": { "$ref": "#/definitions/__schema147" },
            "security": { "$ref": "#/definitions/__schema353" },
            "tlsSettings": { "$ref": "#/definitions/__schema354" },
            "finalmask": { "$ref": "#/definitions/__schema195" },
            "sockopt": { "$ref": "#/definitions/__schema296" }
          },
          "required": ["network", "security", "tlsSettings"],
          "additionalProperties": false
        },
        {
          "type": "object",
          "properties": {
            "network": { "$ref": "#/definitions/__schema405" },
            "tcpSettings": { "$ref": "#/definitions/__schema147" },
            "security": { "$ref": "#/definitions/__schema169" },
            "realitySettings": { "$ref": "#/definitions/__schema170" },
            "finalmask": { "$ref": "#/definitions/__schema195" },
            "sockopt": { "$ref": "#/definitions/__schema296" }
          },
          "required": ["network", "security", "realitySettings"],
          "additionalProperties": false
        },
        {
          "type": "object",
          "properties": {
            "network": { "$ref": "#/definitions/__schema405" },
            "tcpSettings": { "$ref": "#/definitions/__schema147" },
            "security": { "$ref": "#/definitions/__schema353" },
            "tlsSettings": { "$ref": "#/definitions/__schema354" },
            "finalmask": { "$ref": "#/definitions/__schema195" },
            "sockopt": { "$ref": "#/definitions/__schema296" }
          },
          "required": ["network", "security", "tlsSettings"],
          "additionalProperties": false
        },
        {
          "type": "object",
          "properties": {
            "network": { "$ref": "#/definitions/__schema406" },
            "kcpSettings": { "$ref": "#/definitions/__schema407" },
            "security": { "$ref": "#/definitions/__schema169" },
            "realitySettings": { "$ref": "#/definitions/__schema170" },
            "finalmask": { "$ref": "#/definitions/__schema195" },
            "sockopt": { "$ref": "#/definitions/__schema296" }
          },
          "required": ["network", "security", "realitySettings"],
          "additionalProperties": false
        },
        {
          "type": "object",
          "properties": {
            "network": { "$ref": "#/definitions/__schema406" },
            "kcpSettings": { "$ref": "#/definitions/__schema407" },
            "security": { "$ref": "#/definitions/__schema353" },
            "tlsSettings": { "$ref": "#/definitions/__schema354" },
            "finalmask": { "$ref": "#/definitions/__schema195" },
            "sockopt": { "$ref": "#/definitions/__schema296" }
          },
          "required": ["network", "security", "tlsSettings"],
          "additionalProperties": false
        },
        {
          "type": "object",
          "properties": {
            "network": { "$ref": "#/definitions/__schema416" },
            "wsSettings": { "$ref": "#/definitions/__schema417" },
            "security": { "$ref": "#/definitions/__schema169" },
            "realitySettings": { "$ref": "#/definitions/__schema170" },
            "finalmask": { "$ref": "#/definitions/__schema195" },
            "sockopt": { "$ref": "#/definitions/__schema296" }
          },
          "required": ["network", "security", "realitySettings"],
          "additionalProperties": false
        },
        {
          "type": "object",
          "properties": {
            "network": { "$ref": "#/definitions/__schema416" },
            "wsSettings": { "$ref": "#/definitions/__schema417" },
            "security": { "$ref": "#/definitions/__schema353" },
            "tlsSettings": { "$ref": "#/definitions/__schema354" },
            "finalmask": { "$ref": "#/definitions/__schema195" },
            "sockopt": { "$ref": "#/definitions/__schema296" }
          },
          "required": ["network", "security", "tlsSettings"],
          "additionalProperties": false
        },
        {
          "type": "object",
          "properties": {
            "network": { "$ref": "#/definitions/__schema424" },
            "grpcSettings": { "$ref": "#/definitions/__schema425" },
            "security": { "$ref": "#/definitions/__schema169" },
            "realitySettings": { "$ref": "#/definitions/__schema170" },
            "finalmask": { "$ref": "#/definitions/__schema195" },
            "sockopt": { "$ref": "#/definitions/__schema296" }
          },
          "required": ["network", "security", "realitySettings"],
          "additionalProperties": false
        },
        {
          "type": "object",
          "properties": {
            "network": { "$ref": "#/definitions/__schema424" },
            "grpcSettings": { "$ref": "#/definitions/__schema425" },
            "security": { "$ref": "#/definitions/__schema353" },
            "tlsSettings": { "$ref": "#/definitions/__schema354" },
            "finalmask": { "$ref": "#/definitions/__schema195" },
            "sockopt": { "$ref": "#/definitions/__schema296" }
          },
          "required": ["network", "security", "tlsSettings"],
          "additionalProperties": false
        },
        {
          "type": "object",
          "properties": {
            "network": { "$ref": "#/definitions/__schema435" },
            "hysteriaSettings": { "$ref": "#/definitions/__schema436" },
            "security": { "$ref": "#/definitions/__schema169" },
            "realitySettings": { "$ref": "#/definitions/__schema170" },
            "finalmask": { "$ref": "#/definitions/__schema195" },
            "sockopt": { "$ref": "#/definitions/__schema296" }
          },
          "required": ["network", "security", "realitySettings"],
          "additionalProperties": false
        },
        {
          "type": "object",
          "properties": {
            "network": { "$ref": "#/definitions/__schema435" },
            "hysteriaSettings": { "$ref": "#/definitions/__schema436" },
            "security": { "$ref": "#/definitions/__schema353" },
            "tlsSettings": { "$ref": "#/definitions/__schema354" },
            "finalmask": { "$ref": "#/definitions/__schema195" },
            "sockopt": { "$ref": "#/definitions/__schema296" }
          },
          "required": ["network", "security", "tlsSettings"],
          "additionalProperties": false
        },
        {
          "type": "object",
          "properties": {
            "network": { "$ref": "#/definitions/__schema456" },
            "httpupgradeSettings": { "$ref": "#/definitions/__schema457" },
            "security": { "$ref": "#/definitions/__schema169" },
            "realitySettings": { "$ref": "#/definitions/__schema170" },
            "finalmask": { "$ref": "#/definitions/__schema195" },
            "sockopt": { "$ref": "#/definitions/__schema296" }
          },
          "required": ["network", "security", "realitySettings"],
          "additionalProperties": false
        },
        {
          "type": "object",
          "properties": {
            "network": { "$ref": "#/definitions/__schema456" },
            "httpupgradeSettings": { "$ref": "#/definitions/__schema457" },
            "security": { "$ref": "#/definitions/__schema353" },
            "tlsSettings": { "$ref": "#/definitions/__schema354" },
            "finalmask": { "$ref": "#/definitions/__schema195" },
            "sockopt": { "$ref": "#/definitions/__schema296" }
          },
          "required": ["network", "security", "tlsSettings"],
          "additionalProperties": false
        },
        {
          "type": "object",
          "properties": {
            "network": { "$ref": "#/definitions/__schema463" },
            "xhttpSettings": { "$ref": "#/definitions/__schema464" },
            "security": { "$ref": "#/definitions/__schema169" },
            "realitySettings": { "$ref": "#/definitions/__schema170" },
            "finalmask": { "$ref": "#/definitions/__schema195" },
            "sockopt": { "$ref": "#/definitions/__schema296" }
          },
          "required": ["network", "security", "realitySettings"],
          "additionalProperties": false
        },
        {
          "type": "object",
          "properties": {
            "network": { "$ref": "#/definitions/__schema463" },
            "xhttpSettings": { "$ref": "#/definitions/__schema464" },
            "security": { "$ref": "#/definitions/__schema353" },
            "tlsSettings": { "$ref": "#/definitions/__schema354" },
            "finalmask": { "$ref": "#/definitions/__schema195" },
            "sockopt": { "$ref": "#/definitions/__schema296" }
          },
          "required": ["network", "security", "tlsSettings"],
          "additionalProperties": false
        }
      ]
    },
    "__schema146": { "type": "string", "const": "raw" },
    "__schema147": {
      "markdownDescription": "`RawObject` corresponds to the `rawSettings` item in [StreamSettingsObject](https://xtls.github.io/en/config/transport.html#streamsettingsobject).\n\n```json\n{\n  // outbound example; also applies to inbound\n  \"outbounds\": [\n    {\n      // ...\n      \"streamSettings\": {\n        \"network\": \"raw\",\n        \"rawSettings\": {\n          \"acceptProxyProtocol\": false,\n          \"header\": {\n            \"type\": \"none\"\n          }\n        }\n      }\n    }\n  ]\n}\n```\n",
      "allOf": [{ "$ref": "#/definitions/__schema148" }]
    },
    "__schema148": {
      "type": "object",
      "properties": {
        "acceptProxyProtocol": {
          "markdownDescription": "Only used for inbound; indicates whether to accept PROXY protocol.\n\n[PROXY protocol](https://www.haproxy.org/download/2.2/doc/proxy-protocol.txt) is dedicated to passing the real source IP and port of the request. If you don't know what it is, please ignore this item for now.\n\nCommon reverse proxy software (such as HAProxy, Nginx) can be configured to send it. VLESS fallbacks xver can also send it.\n\nWhen set to `true`, after the underlying TCP connection is established, the requester must send PROXY protocol v1 or v2 first; otherwise, the connection will be closed.\n\nDefault value is `false`.\n",
          "allOf": [{ "$ref": "#/definitions/__schema149" }]
        },
        "header": {
          "markdownDescription": "Packet header obfuscation settings. Default value is `NoneHeaderObject`.\n\n### TIP\n\nHTTP obfuscation cannot be traffic-split by other HTTP servers (like Nginx), but can be split by VLESS fallbacks path.\n",
          "allOf": [{ "$ref": "#/definitions/__schema150" }]
        }
      },
      "additionalProperties": false
    },
    "__schema149": { "default": false, "type": "boolean" },
    "__schema150": {
      "default": { "type": "none" },
      "oneOf": [
        {
          "type": "object",
          "properties": {
            "type": {
              "type": "string",
              "const": "none",
              "markdownDescription": "No obfuscation.\n\n```json\n{\n  \"type\": \"none\"\n}\n```\n\n`type`: \"none\"\n\nSpecifies no obfuscation.\n"
            }
          },
          "required": ["type"],
          "additionalProperties": false
        },
        {
          "type": "object",
          "properties": {
            "type": {
              "type": "string",
              "const": "http",
              "markdownDescription": "HTTP obfuscation configuration must be configured on the corresponding inbound and outbound connections simultaneously, and the content must be consistent.\n\n```json\n{\n  \"type\": \"http\",\n  \"request\": {},\n  \"response\": {}\n}\n```\n\n`type`: \"http\"\n\nSpecifies HTTP obfuscation.\n"
            },
            "request": { "allOf": [{ "$ref": "#/definitions/__schema151" }] },
            "response": { "allOf": [{ "$ref": "#/definitions/__schema160" }] }
          },
          "required": ["type"],
          "additionalProperties": false
        }
      ]
    },
    "__schema151": {
      "default": {},
      "type": "object",
      "properties": {
        "version": { "$ref": "#/definitions/__schema152" },
        "method": { "$ref": "#/definitions/__schema154" },
        "path": { "$ref": "#/definitions/__schema156" },
        "headers": { "$ref": "#/definitions/__schema158" }
      },
      "additionalProperties": false,
      "markdownDescription": "HTTP Request.\n"
    },
    "__schema152": {
      "markdownDescription": "HTTP version. Default value is `\"1.1\"`.\n",
      "allOf": [{ "$ref": "#/definitions/__schema153" }]
    },
    "__schema153": { "default": "1.1", "type": "string" },
    "__schema154": {
      "markdownDescription": "HTTP method. Default value is `\"GET\"`.\n",
      "allOf": [{ "$ref": "#/definitions/__schema155" }]
    },
    "__schema155": { "default": "GET", "type": "string" },
    "__schema156": {
      "markdownDescription": "Path, an array of strings. Default value is `[\"/\"]`. When there are multiple values, one is randomly selected for each request.\n",
      "allOf": [{ "$ref": "#/definitions/__schema157" }]
    },
    "__schema157": { "default": ["/"], "type": "array", "items": { "type": "string" } },
    "__schema158": {
      "markdownDescription": "HTTP headers. A key-value pair, where each key represents the name of an HTTP header, and the corresponding value is an array.\n\nAll keys will be attached to every request, and one corresponding value will be randomly selected. See the example above for default values.\n",
      "allOf": [{ "$ref": "#/definitions/__schema159" }]
    },
    "__schema159": {
      "default": {},
      "type": "object",
      "propertyNames": { "type": "string" },
      "additionalProperties": {
        "anyOf": [{ "type": "string" }, { "type": "array", "items": { "type": "string" } }]
      }
    },
    "__schema160": {
      "default": {},
      "type": "object",
      "properties": {
        "version": { "$ref": "#/definitions/__schema161" },
        "status": { "$ref": "#/definitions/__schema163" },
        "reason": { "$ref": "#/definitions/__schema165" },
        "headers": { "$ref": "#/definitions/__schema167" }
      },
      "additionalProperties": false,
      "markdownDescription": "HTTP Response.\n"
    },
    "__schema161": {
      "markdownDescription": "HTTP version. Default value is `\"1.1\"`.\n",
      "allOf": [{ "$ref": "#/definitions/__schema162" }]
    },
    "__schema162": { "default": "1.1", "type": "string" },
    "__schema163": {
      "markdownDescription": "HTTP status code. Default value is `\"200\"`.\n",
      "allOf": [{ "$ref": "#/definitions/__schema164" }]
    },
    "__schema164": { "default": "200", "type": "string" },
    "__schema165": {
      "markdownDescription": "HTTP status reason phrase. Default value is `\"OK\"`.\n",
      "allOf": [{ "$ref": "#/definitions/__schema166" }]
    },
    "__schema166": { "default": "OK", "type": "string" },
    "__schema167": {
      "markdownDescription": "HTTP headers. A key-value pair, where each key represents the name of an HTTP header, and the corresponding value is an array.\n\nAll keys will be attached to every request, and one corresponding value will be randomly selected. See the example above for default values.\n",
      "allOf": [{ "$ref": "#/definitions/__schema168" }]
    },
    "__schema168": {
      "default": {},
      "type": "object",
      "propertyNames": { "type": "string" },
      "additionalProperties": {
        "anyOf": [{ "type": "string" }, { "type": "array", "items": { "type": "string" } }]
      }
    },
    "__schema169": { "type": "string", "const": "reality" },
    "__schema170": {
      "anyOf": [{ "$ref": "#/definitions/__schema171" }, { "$ref": "#/definitions/__schema185" }],
      "markdownDescription": "`RealityObject` corresponds to the `realitySettings` item in `StreamSettingsObject`.\n\nThe object supports both inbound (server-side) settings and outbound (client-side) settings.\n\n[Documentation ↗](https://xtls.github.io/en/config/transports/reality.html)\n"
    },
    "__schema171": {
      "type": "object",
      "properties": {
        "show": {
          "markdownDescription": "When set to `true`, debug information is printed.\n\n### TIP\n\nThis field is for inbound (server-side) configuration.\n",
          "allOf": [{ "$ref": "#/definitions/__schema172" }]
        },
        "target": {
          "type": "string",
          "minLength": 1,
          "markdownDescription": "Required inbound (server-side) target destination.\n\nThe format is the same as VLESS fallback `dest`. The old name was `dest`; in current versions the two fields are aliases.\n\nThe core decides whether the current configuration is client-side or server-side based on whether this field exists. Do not fill it in on the client side, or it will cause incorrect detection.\n\nIf `target` supports the post-quantum key-exchange algorithm `X25519MLKEM768`, the REALITY client also automatically uses that algorithm for key negotiation.\n\nYou can check support with `xray tls ping cloudflare.com`, replacing the domain with your `target` and optionally including a port.\n\n### WARNING\n\nFor camouflage reasons, Xray directly forwards traffic that fails authentication to `target`. If the IP address of the target site is special, your server may effectively become a port forwarder for that service and may be abused after scanning.\n"
        },
        "xver": {
          "markdownDescription": "Optional version field for fallback behavior.\n\nThe format is the same as VLESS fallback `xver`.\n",
          "allOf": [{ "$ref": "#/definitions/__schema173" }]
        },
        "serverNames": {
          "minItems": 1,
          "type": "array",
          "items": { "$ref": "#/definitions/__schema174" },
          "markdownDescription": "Required inbound (server-side) list of allowed `serverName` values for clients.\n\n`*` wildcards are not supported.\n\nUsually this should stay consistent with `target`. Valid values are any SNI accepted by the server according to the behavior of `target`, and typically refer to the SAN values on the returned certificate.\n\nThe list may contain an empty string `\"\"`, meaning connections without SNI are accepted. In that case, the client-side `serverName` cannot be empty and should instead be filled with any valid IP address as a placeholder.\n"
        },
        "privateKey": {
          "type": "string",
          "minLength": 1,
          "markdownDescription": "Required server-side private key.\n\nGenerate it with `./xray x25519`.\n"
        },
        "minClientVer": {
          "markdownDescription": "Optional minimum Xray client version in `x.y.z` format.\n",
          "allOf": [{ "$ref": "#/definitions/__schema175" }]
        },
        "maxClientVer": {
          "markdownDescription": "Optional maximum Xray client version in `x.y.z` format.\n",
          "allOf": [{ "$ref": "#/definitions/__schema176" }]
        },
        "maxTimeDiff": {
          "markdownDescription": "Optional maximum allowed time difference, in milliseconds.\n",
          "allOf": [{ "$ref": "#/definitions/__schema177" }]
        },
        "shortIds": {
          "minItems": 1,
          "type": "array",
          "items": { "$ref": "#/definitions/__schema178" },
          "markdownDescription": "Required server-side list of available `shortId` values for clients.\n\nSee the `shortId` field for the required format.\n\nIf the list contains an empty string, the client-side `shortId` may also be empty.\n"
        },
        "mldsa65Seed": {
          "markdownDescription": "Server-side private key used to add an extra post-quantum signature to the certificate sent to the REALITY client, using ML-DSA-65.\n\nGenerate the keypair with `xray mldsa65`.\n\nAfter the server is configured with the private key, the signature is added only as a certificate extension, so it does not affect old clients or clients that do not enable this feature.\n\nThe target certificate must be larger than 3500 bytes because the post-quantum signature makes the temporary REALITY certificate larger.\n\nTo avoid becoming a fingerprint, the `target` certificate also needs to be large.\n",
          "allOf": [{ "$ref": "#/definitions/__schema179" }]
        },
        "limitFallbackUpload": {
          "markdownDescription": "Optional rate limit for fallback REALITY connections that fail verification.\n\nThis object applies to upload traffic.\n\nThe rate limiter starts after the specified number of bytes has been transmitted. The bucket capacity is `burstBytesPerSec`. Each transmitted byte consumes one token. The bucket starts full with `burstBytesPerSec` tokens and is refilled with `bytesPerSec` tokens every second until full.\n\n### WARNING\n\nFallback rate limiting is itself a fingerprint and is not recommended unless you need it.\n",
          "allOf": [{ "$ref": "#/definitions/__schema180" }]
        },
        "limitFallbackDownload": {
          "markdownDescription": "Optional rate limit for fallback REALITY connections that fail verification.\n\nThis object applies to download traffic.\n\nThe rate limiter starts after the specified number of bytes has been transmitted. The bucket capacity is `burstBytesPerSec`. Each transmitted byte consumes one token. The bucket starts full with `burstBytesPerSec` tokens and is refilled with `bytesPerSec` tokens every second until full.\n\n### WARNING\n\nFallback rate limiting is itself a fingerprint and is not recommended unless you need it.\n",
          "allOf": [{ "$ref": "#/definitions/__schema180" }]
        }
      },
      "required": ["target", "serverNames", "privateKey", "shortIds"],
      "additionalProperties": false
    },
    "__schema172": { "default": false, "type": "boolean" },
    "__schema173": { "default": 0, "type": "number" },
    "__schema174": { "type": "string" },
    "__schema175": { "type": "string" },
    "__schema176": { "type": "string" },
    "__schema177": { "default": 0, "type": "integer", "minimum": 0, "maximum": 9007199254740991 },
    "__schema178": { "type": "string" },
    "__schema179": { "default": "", "type": "string" },
    "__schema180": {
      "markdownDescription": "Optional rate-limit settings for fallback REALITY connections that fail verification.\n\nThis object is used for both upload and download fallback limits.\n\nThe limiter starts after the specified number of bytes has been transmitted. The bucket capacity is `burstBytesPerSec`. Each transmitted byte consumes one token. The bucket starts full with `burstBytesPerSec` tokens and is refilled with `bytesPerSec` tokens every second until full.\n\n### WARNING\n\nFallback rate limiting is itself a fingerprint and is not recommended unless you need it.\n",
      "allOf": [{ "$ref": "#/definitions/__schema181" }]
    },
    "__schema181": {
      "type": "object",
      "properties": {
        "afterBytes": {
          "markdownDescription": "Starts rate limiting fallback REALITY connections only after the specified number of bytes has been transmitted. The default is `0`.\n",
          "allOf": [{ "$ref": "#/definitions/__schema182" }]
        },
        "bytesPerSec": {
          "markdownDescription": "Base rate limit for fallback REALITY connections, in bytes per second. The default is `0`, meaning disabled.\n",
          "allOf": [{ "$ref": "#/definitions/__schema183" }]
        },
        "burstBytesPerSec": {
          "markdownDescription": "Burst rate limit for fallback REALITY connections, in bytes per second. It takes effect when it is greater than `bytesPerSec`.\n\nIf you do not want bursts, set this field to `0`.\n",
          "allOf": [{ "$ref": "#/definitions/__schema184" }]
        }
      },
      "additionalProperties": false
    },
    "__schema182": { "default": 0, "type": "integer", "minimum": 0, "maximum": 9007199254740991 },
    "__schema183": { "default": 0, "type": "integer", "minimum": 0, "maximum": 9007199254740991 },
    "__schema184": { "default": 0, "type": "integer", "minimum": 0, "maximum": 9007199254740991 },
    "__schema185": {
      "anyOf": [
        {
          "type": "object",
          "properties": {
            "serverName": { "$ref": "#/definitions/__schema186" },
            "fingerprint": { "$ref": "#/definitions/__schema188" },
            "shortId": { "$ref": "#/definitions/__schema190" },
            "mldsa65Verify": { "$ref": "#/definitions/__schema191" },
            "spiderX": { "$ref": "#/definitions/__schema193" },
            "password": {
              "type": "string",
              "markdownDescription": "Required outbound client-side public key corresponding to the server private key.\n\nGenerate it with `./xray x25519 -i \"server private key\"`.\n\nThe old field name was `publicKey`, but it was renamed to avoid misunderstanding.\n"
            }
          },
          "required": ["shortId", "password"],
          "additionalProperties": false
        },
        {
          "type": "object",
          "properties": {
            "serverName": { "$ref": "#/definitions/__schema186" },
            "fingerprint": { "$ref": "#/definitions/__schema188" },
            "shortId": { "$ref": "#/definitions/__schema190" },
            "mldsa65Verify": { "$ref": "#/definitions/__schema191" },
            "spiderX": { "$ref": "#/definitions/__schema193" },
            "publicKey": {
              "type": "string",
              "markdownDescription": "Deprecated alias for the outbound client-side public key corresponding to the server private key.\n\nGenerate it with `./xray x25519 -i \"server private key\"`.\n\nUse `password` instead.\n"
            }
          },
          "required": ["shortId", "publicKey"],
          "additionalProperties": false
        }
      ]
    },
    "__schema186": {
      "markdownDescription": "Outbound client-side `serverName`.\n\nOne of the server-side `serverNames`.\n\nThe client can also set this to any IP address. In that case Xray sends a Client Hello without an SNI extension. To use this feature, the server-side `serverNames` must contain an empty string `\"\"`.\n",
      "allOf": [{ "$ref": "#/definitions/__schema187" }]
    },
    "__schema187": { "type": "string" },
    "__schema188": {
      "markdownDescription": "Required outbound client-side fingerprint.\n\nSame as `TLSObject`. Note that `unsafe`, which disables uTLS for TLS, is not supported here because REALITY relies on that library to manipulate lower-level TLS parameters.\n\n### TIP\n\nWhen enabled, some TLS options that affect TLS fingerprints are overridden by the underlying uTLS behavior.\n",
      "allOf": [{ "$ref": "#/definitions/__schema189" }]
    },
    "__schema189": {
      "anyOf": [
        {
          "type": "string",
          "enum": [
            "360",
            "chrome",
            "firefox",
            "safari",
            "ios",
            "android",
            "edge",
            "qq",
            "random",
            "randomized"
          ]
        },
        { "type": "string" }
      ]
    },
    "__schema190": {
      "type": "string",
      "markdownDescription": "Outbound client-side `shortId`.\n\nOne of the server-side `shortIds`.\n\nIts length is 8 bytes, which means up to 16 hexadecimal characters in the range `0` to `f`. It may be shorter than 16 characters, and the core pads trailing zeroes automatically, but the number of characters must be even.\n\nIf the server-side `shortIds` contains an empty string, the client-side value may also be empty.\n"
    },
    "__schema191": {
      "markdownDescription": "Optional public key used for `mldsa65` signature verification.\n\nWhen non-empty, Xray uses it to verify the certificate returned by the server.\n",
      "allOf": [{ "$ref": "#/definitions/__schema192" }]
    },
    "__schema192": { "type": "string" },
    "__schema193": {
      "markdownDescription": "Initial crawler path and parameters.\n\nIt is recommended that each client use a different value.\n",
      "allOf": [{ "$ref": "#/definitions/__schema194" }]
    },
    "__schema194": { "type": "string" },
    "__schema195": {
      "type": "object",
      "properties": {
        "tcp": { "$ref": "#/definitions/__schema196" },
        "udp": { "$ref": "#/definitions/__schema231" },
        "quicParams": { "$ref": "#/definitions/__schema267" }
      },
      "additionalProperties": false,
      "markdownDescription": "FinalMask performs the last stage of traffic obfuscation after the core has already processed transport-layer security, including TLS and REALITY.\n\nIt can be used for multiple kinds of TCP and UDP camouflage, as well as QUIC-related parameter tuning.\n\n[Documentation ↗](https://xtls.github.io/en/config/transports/finalmask.html)\n"
    },
    "__schema196": {
      "markdownDescription": "`FinalMaskObject` corresponds to the `finalmask` item in `StreamSettingsObject`.\n\n[Documentation ↗](https://xtls.github.io/en/config/transports/finalmask.html)\n",
      "allOf": [{ "$ref": "#/definitions/__schema197" }]
    },
    "__schema197": {
      "type": "array",
      "items": { "$ref": "#/definitions/__schema198" },
      "markdownDescription": "TCP FinalMask layers are applied in order, with the first item being the outermost camouflage layer.\n\nUsed together with `raw`, `httpupgrade`, `websocket`, `grpc`, and `xhttp`.\n\nSupported TCP layer types:\n\n- `header-custom`\n- `fragment`\n- `sudoku`\n"
    },
    "__schema198": {
      "oneOf": [
        {
          "type": "object",
          "properties": {
            "type": { "$ref": "#/definitions/__schema199" },
            "settings": { "$ref": "#/definitions/__schema200" }
          },
          "required": ["type", "settings"],
          "additionalProperties": false,
          "markdownDescription": "TCP `header-custom` uses layered client, server, and error packet sequences.\n\nEach outer array entry is a camouflage layer, and each inner entry is a `header-custom` packet object defined by `customSettingsObject`.\n"
        },
        {
          "type": "object",
          "properties": {
            "type": { "$ref": "#/definitions/__schema215" },
            "settings": { "$ref": "#/definitions/__schema216" }
          },
          "required": ["type", "settings"],
          "additionalProperties": false,
          "markdownDescription": "A TCP fragmentation layer.\n\nFields:\n\n- `packets`: packet preset. Supported values are `1-3` and `tlshello`.\n- `length`: packet length or range.\n- `delay`: delay range in milliseconds.\n- `maxSplit`: maximum split count range.\n\nThe project ships a snippet for `tlshello` with `100-200` length, `10-20` delay, and `3-6` max split.\n"
        },
        { "$ref": "#/definitions/__schema221" }
      ]
    },
    "__schema199": {
      "type": "string",
      "const": "header-custom",
      "markdownDescription": "TCP FinalMask layer type.\n\nSupported values are `header-custom`, `fragment`, and `sudoku`.\n"
    },
    "__schema200": {
      "type": "object",
      "properties": {
        "clients": { "$ref": "#/definitions/__schema201" },
        "servers": { "$ref": "#/definitions/__schema213" },
        "errors": { "$ref": "#/definitions/__schema214" }
      },
      "required": ["clients", "servers", "errors"],
      "additionalProperties": false,
      "markdownDescription": "TCP `header-custom` uses layered client, server, and error packet sequences.\n\nEach outer array entry is a camouflage layer, and each inner entry is a `header-custom` packet object defined by `customSettingsObject`.\n"
    },
    "__schema201": {
      "type": "array",
      "items": { "type": "array", "items": { "$ref": "#/definitions/__schema202" } }
    },
    "__schema202": {
      "type": "object",
      "properties": {
        "delay": { "$ref": "#/definitions/__schema203" },
        "rand": { "$ref": "#/definitions/__schema205" },
        "randRange": { "$ref": "#/definitions/__schema207" },
        "type": { "$ref": "#/definitions/__schema209" },
        "packet": { "$ref": "#/definitions/__schema211" }
      },
      "additionalProperties": false,
      "markdownDescription": "`header-custom` settings object used by FinalMask.\n\nIt defines one packet layer entry with optional delay, random padding, packet encoding type, and fixed packet data.\n\nFields:\n\n- `delay`: delay in milliseconds. When it is `0`, the data is sent together with the previous packet.\n- `rand`: adds a specified number of random bytes. Conflicts with `packet`.\n- `randRange`: range of random-byte values. The default is `0-255`.\n- `type`: the type of `packet`. Supported values are `array`, `str`, `hex`, and `base64`. The default is `array`.\n- `packet`: adds fixed data. Conflicts with `rand`.\n"
    },
    "__schema203": {
      "markdownDescription": "Delay in milliseconds. When it is `0`, the data is sent together with the previous packet.\n",
      "allOf": [{ "$ref": "#/definitions/__schema204" }]
    },
    "__schema204": {
      "default": 0,
      "type": "integer",
      "minimum": -9007199254740991,
      "maximum": 9007199254740991
    },
    "__schema205": {
      "markdownDescription": "Adds a specified number of random bytes. Conflicts with `packet`.\n",
      "allOf": [{ "$ref": "#/definitions/__schema206" }]
    },
    "__schema206": {
      "default": 0,
      "type": "integer",
      "minimum": -9007199254740991,
      "maximum": 9007199254740991
    },
    "__schema207": {
      "markdownDescription": "Range of random-byte values. The default is `0-255`.\n",
      "allOf": [{ "$ref": "#/definitions/__schema208" }]
    },
    "__schema208": { "default": "0-255", "type": "string" },
    "__schema209": {
      "markdownDescription": "Type of `packet`. Supported values are `array`, `str`, `hex`, and `base64`. The default is `array`.\n",
      "allOf": [{ "$ref": "#/definitions/__schema210" }]
    },
    "__schema210": {
      "default": "array",
      "type": "string",
      "enum": ["array", "str", "hex", "base64"]
    },
    "__schema211": {
      "markdownDescription": "Adds fixed data. Conflicts with `rand`.\n",
      "allOf": [{ "$ref": "#/definitions/__schema212" }]
    },
    "__schema212": { "type": "array", "items": {} },
    "__schema213": {
      "type": "array",
      "items": { "type": "array", "items": { "$ref": "#/definitions/__schema202" } }
    },
    "__schema214": {
      "type": "array",
      "items": { "type": "array", "items": { "$ref": "#/definitions/__schema202" } }
    },
    "__schema215": {
      "type": "string",
      "const": "fragment",
      "markdownDescription": "TCP FinalMask layer type.\n\nSupported values are `header-custom`, `fragment`, and `sudoku`.\n"
    },
    "__schema216": {
      "type": "object",
      "properties": {
        "packets": { "$ref": "#/definitions/__schema217" },
        "length": { "$ref": "#/definitions/__schema218" },
        "delay": { "$ref": "#/definitions/__schema219" },
        "maxSplit": { "$ref": "#/definitions/__schema220" }
      },
      "required": ["packets", "length", "delay", "maxSplit"],
      "additionalProperties": false,
      "defaultSnippets": [
        {
          "label": "tlshello 100-200",
          "description": "a fragmentation example from docs",
          "body": {
            "packets": "tlshello",
            "length": "100-200",
            "delay": "10-20",
            "maxSplit": "3-6"
          }
        }
      ],
      "markdownDescription": "A TCP fragmentation layer.\n\nFields:\n\n- `packets`: packet preset. Supported values are `1-3` and `tlshello`.\n- `length`: packet length or range.\n- `delay`: delay range in milliseconds.\n- `maxSplit`: maximum split count range.\n\nThe project ships a snippet for `tlshello` with `100-200` length, `10-20` delay, and `3-6` max split.\n"
    },
    "__schema217": {
      "type": "string",
      "enum": ["1-3", "tlshello"],
      "markdownDescription": "Packet preset for the TCP fragment layer.\n\nSupported values are `1-3` and `tlshello`.\n"
    },
    "__schema218": {
      "type": "string",
      "markdownDescription": "Packet length or range used by the TCP fragment layer.\n"
    },
    "__schema219": {
      "type": "string",
      "markdownDescription": "Delay in milliseconds. When it is `0`, the data is sent together with the previous packet.\n"
    },
    "__schema220": {
      "type": "string",
      "markdownDescription": "Maximum split count range used by the TCP fragment layer.\n"
    },
    "__schema221": {
      "type": "object",
      "properties": {
        "type": { "$ref": "#/definitions/__schema222" },
        "settings": { "$ref": "#/definitions/__schema223" }
      },
      "required": ["type", "settings"],
      "additionalProperties": false,
      "markdownDescription": "FinalMask Sudoku camouflage layer.\n\nThe same meanings as in the TCP and UDP versions apply here.\n\nUse together with the upstream Sudoku ASCII configuration format.\n\n[Documentation ↗](https://xtls.github.io/en/config/transports/finalmask.html#sudoku)\n"
    },
    "__schema222": { "type": "string", "const": "sudoku" },
    "__schema223": {
      "type": "object",
      "properties": {
        "password": { "$ref": "#/definitions/__schema224" },
        "ascii": { "$ref": "#/definitions/__schema225" },
        "customTable": { "$ref": "#/definitions/__schema226" },
        "customTables": { "$ref": "#/definitions/__schema227" },
        "paddingMin": { "$ref": "#/definitions/__schema229" },
        "paddingMax": { "$ref": "#/definitions/__schema230" }
      },
      "required": ["password", "ascii", "customTable", "customTables", "paddingMin", "paddingMax"],
      "additionalProperties": false,
      "markdownDescription": "FinalMask Sudoku camouflage layer.\n\nThe same meanings as in the TCP and UDP versions apply here.\n\nUse together with the upstream Sudoku ASCII configuration format.\n\n[Documentation ↗](https://xtls.github.io/en/config/transports/finalmask.html#sudoku)\n"
    },
    "__schema224": {
      "type": "string",
      "markdownDescription": "Password for Sudoku and related camouflage settings.\n"
    },
    "__schema225": {
      "type": "string",
      "markdownDescription": "ASCII mapping or symbol table used by Sudoku camouflage.\n"
    },
    "__schema226": {
      "type": "string",
      "markdownDescription": "Custom table used by Sudoku camouflage.\n"
    },
    "__schema227": {
      "type": "array",
      "items": { "$ref": "#/definitions/__schema228" },
      "markdownDescription": "Custom tables used by Sudoku camouflage.\n"
    },
    "__schema228": { "type": "string" },
    "__schema229": {
      "type": "integer",
      "minimum": -9007199254740991,
      "maximum": 9007199254740991,
      "markdownDescription": "Minimum padding value used by Sudoku camouflage.\n"
    },
    "__schema230": {
      "type": "integer",
      "minimum": -9007199254740991,
      "maximum": 9007199254740991,
      "markdownDescription": "Maximum padding value used by Sudoku camouflage.\n"
    },
    "__schema231": {
      "markdownDescription": "`FinalMaskObject` corresponds to the `finalmask` item in `StreamSettingsObject`.\n\n[Documentation ↗](https://xtls.github.io/en/config/transports/finalmask.html)\n",
      "allOf": [{ "$ref": "#/definitions/__schema232" }]
    },
    "__schema232": {
      "type": "array",
      "items": { "$ref": "#/definitions/__schema233" },
      "markdownDescription": "UDP FinalMask layers are applied in order, with the first item being the outermost camouflage layer.\n\nUsed together with `raw` UDP, `kcp`, `hysteria`, and `xhttp` H3.\n\nSupported UDP layer types:\n\n- `header-custom`\n- `header-dns`\n- `header-dtls`\n- `header-srtp`\n- `header-utp`\n- `header-wechat`\n- `header-wireguard`\n- `mkcp-original`\n- `mkcp-aes128gcm`\n- `noise`\n- `salamander`\n- `sudoku`\n- `xdns`\n- `xicmp`\n"
    },
    "__schema233": {
      "oneOf": [
        {
          "type": "object",
          "properties": {
            "type": { "$ref": "#/definitions/__schema234" },
            "settings": { "$ref": "#/definitions/__schema235" }
          },
          "required": ["type", "settings"],
          "additionalProperties": false,
          "markdownDescription": "UDP `header-custom` uses layered client and server packet sequences.\n\nEach inner entry is a `header-custom` packet object defined by `customSettingsObject`.\n"
        },
        {
          "type": "object",
          "properties": {
            "type": { "$ref": "#/definitions/__schema238" },
            "settings": { "$ref": "#/definitions/__schema239" }
          },
          "required": ["type", "settings"],
          "additionalProperties": false,
          "markdownDescription": "UDP DNS camouflage header.\n\n`domain` is the DNS name used by the camouflage layer.\n"
        },
        {
          "type": "object",
          "properties": {
            "type": { "$ref": "#/definitions/__schema241" },
            "settings": { "$ref": "#/definitions/__schema242" }
          },
          "required": ["type", "settings"],
          "additionalProperties": false,
          "markdownDescription": "Old mKCP `seed` feature compatibility layer.\n\n`password` is the obfuscation password.\n"
        },
        {
          "type": "object",
          "properties": {
            "type": { "$ref": "#/definitions/__schema244" },
            "settings": { "$ref": "#/definitions/__schema245" }
          },
          "required": ["type", "settings"],
          "additionalProperties": false,
          "markdownDescription": "Noise camouflage for UDP.\n\nFields:\n\n- `reset`: reset marker for the noise sequence.\n- `noise`: array of noise packets.\n\nEach noise packet can set random payload size, byte range, packet data, packet type, and delay.\n"
        },
        {
          "type": "object",
          "properties": {
            "type": { "$ref": "#/definitions/__schema255" },
            "settings": { "$ref": "#/definitions/__schema256" }
          },
          "required": ["type", "settings"],
          "additionalProperties": false,
          "markdownDescription": "Salamander obfuscation from Hysteria2.\n\n`password` is the obfuscation password.\n"
        },
        { "$ref": "#/definitions/__schema221" },
        {
          "type": "object",
          "properties": {
            "type": { "$ref": "#/definitions/__schema258" },
            "settings": { "$ref": "#/definitions/__schema259" }
          },
          "required": ["type", "settings"],
          "additionalProperties": false,
          "markdownDescription": "Transmits data through DNS queries in a way similar to DNSTT.\n\n`domain` is the DNS name used for the tunnel.\n"
        },
        {
          "type": "object",
          "properties": {
            "type": { "$ref": "#/definitions/__schema261" },
            "settings": { "$ref": "#/definitions/__schema262" }
          },
          "required": ["type", "settings"],
          "additionalProperties": false,
          "markdownDescription": "ICMP camouflage layer.\n\nFields:\n\n- `listenIp`: the IP address to listen on. Defaults to `0.0.0.0`.\n- `id`: when multiple clients share the same IP, keep this value at `0`.\n"
        }
      ]
    },
    "__schema234": {
      "type": "string",
      "const": "header-custom",
      "markdownDescription": "UDP FinalMask layer type.\n\nSupported values are `header-custom`, `header-dns`, `header-dtls`, `header-srtp`, `header-utp`, `header-wechat`, `header-wireguard`, `mkcp-original`, `mkcp-aes128gcm`, `noise`, `salamander`, `sudoku`, `xdns`, and `xicmp`.\n"
    },
    "__schema235": {
      "type": "object",
      "properties": {
        "client": { "$ref": "#/definitions/__schema236" },
        "server": { "$ref": "#/definitions/__schema237" }
      },
      "required": ["client", "server"],
      "additionalProperties": false,
      "markdownDescription": "UDP `header-custom` uses layered client and server packet sequences.\n\nEach inner entry is a `header-custom` packet object defined by `customSettingsObject`.\n"
    },
    "__schema236": { "type": "array", "items": { "$ref": "#/definitions/__schema202" } },
    "__schema237": { "type": "array", "items": { "$ref": "#/definitions/__schema202" } },
    "__schema238": {
      "type": "string",
      "const": "header-dns",
      "markdownDescription": "UDP FinalMask layer type.\n\nSupported values are `header-custom`, `header-dns`, `header-dtls`, `header-srtp`, `header-utp`, `header-wechat`, `header-wireguard`, `mkcp-original`, `mkcp-aes128gcm`, `noise`, `salamander`, `sudoku`, `xdns`, and `xicmp`.\n"
    },
    "__schema239": {
      "type": "object",
      "properties": { "domain": { "$ref": "#/definitions/__schema240" } },
      "required": ["domain"],
      "additionalProperties": false,
      "markdownDescription": "UDP DNS camouflage header.\n\n`domain` is the DNS name used by the camouflage layer.\n"
    },
    "__schema240": {
      "type": "string",
      "markdownDescription": "Domain name used by the camouflage layer.\n"
    },
    "__schema241": {
      "type": "string",
      "const": "mkcp-aes128gcm",
      "markdownDescription": "UDP FinalMask layer type.\n\nSupported values are `header-custom`, `header-dns`, `header-dtls`, `header-srtp`, `header-utp`, `header-wechat`, `header-wireguard`, `mkcp-original`, `mkcp-aes128gcm`, `noise`, `salamander`, `sudoku`, `xdns`, and `xicmp`.\n"
    },
    "__schema242": {
      "type": "object",
      "properties": { "password": { "$ref": "#/definitions/__schema243" } },
      "required": ["password"],
      "additionalProperties": false,
      "markdownDescription": "Old mKCP `seed` feature compatibility layer.\n\n`password` is the obfuscation password.\n"
    },
    "__schema243": {
      "type": "string",
      "markdownDescription": "Password for Sudoku and related camouflage settings.\n"
    },
    "__schema244": {
      "type": "string",
      "const": "noise",
      "markdownDescription": "UDP FinalMask layer type.\n\nSupported values are `header-custom`, `header-dns`, `header-dtls`, `header-srtp`, `header-utp`, `header-wechat`, `header-wireguard`, `mkcp-original`, `mkcp-aes128gcm`, `noise`, `salamander`, `sudoku`, `xdns`, and `xicmp`.\n"
    },
    "__schema245": {
      "type": "object",
      "properties": {
        "reset": { "$ref": "#/definitions/__schema246" },
        "noise": { "$ref": "#/definitions/__schema247" }
      },
      "required": ["reset", "noise"],
      "additionalProperties": false,
      "markdownDescription": "Noise camouflage for UDP.\n\nFields:\n\n- `reset`: reset marker for the noise sequence.\n- `noise`: array of noise packets.\n\nEach noise packet can set random payload size, byte range, packet data, packet type, and delay.\n"
    },
    "__schema246": {
      "type": "integer",
      "minimum": -9007199254740991,
      "maximum": 9007199254740991,
      "markdownDescription": "Reset marker for the UDP noise sequence.\n"
    },
    "__schema247": {
      "type": "array",
      "items": { "$ref": "#/definitions/__schema248" },
      "markdownDescription": "Noise packet entries used by the UDP noise camouflage layer.\n\nEach entry can set random payload size, byte range, packet type, packet data, and delay.\n"
    },
    "__schema248": {
      "type": "object",
      "properties": {
        "rand": { "$ref": "#/definitions/__schema249" },
        "randRange": { "$ref": "#/definitions/__schema250" },
        "type": { "$ref": "#/definitions/__schema251" },
        "packet": { "$ref": "#/definitions/__schema252" },
        "delay": { "$ref": "#/definitions/__schema254" }
      },
      "required": ["rand", "randRange", "type", "packet", "delay"],
      "additionalProperties": false,
      "defaultSnippets": [
        {
          "label": "noise 1-8192",
          "description": "noise example from the docs",
          "body": {
            "rand": "1-8192",
            "randRange": "0-255",
            "type": "",
            "packet": [],
            "delay": "10-20"
          }
        }
      ],
      "markdownDescription": "Noise packet entries used by the UDP noise camouflage layer.\n\nEach entry can set random payload size, byte range, packet type, packet data, and delay.\n"
    },
    "__schema249": {
      "type": "string",
      "markdownDescription": "Adds a specified number of random bytes. Conflicts with `packet`.\n"
    },
    "__schema250": {
      "type": "string",
      "markdownDescription": "Range of random-byte values. The default is `0-255`.\n"
    },
    "__schema251": {
      "type": "string",
      "markdownDescription": "Type of `packet`. Supported values are `array`, `str`, `hex`, and `base64`. The default is `array`.\n"
    },
    "__schema252": {
      "type": "array",
      "items": { "$ref": "#/definitions/__schema253" },
      "markdownDescription": "Adds fixed data. Conflicts with `rand`.\n"
    },
    "__schema253": {},
    "__schema254": {
      "type": "string",
      "markdownDescription": "Delay in milliseconds. When it is `0`, the data is sent together with the previous packet.\n"
    },
    "__schema255": {
      "type": "string",
      "const": "salamander",
      "markdownDescription": "UDP FinalMask layer type.\n\nSupported values are `header-custom`, `header-dns`, `header-dtls`, `header-srtp`, `header-utp`, `header-wechat`, `header-wireguard`, `mkcp-original`, `mkcp-aes128gcm`, `noise`, `salamander`, `sudoku`, `xdns`, and `xicmp`.\n"
    },
    "__schema256": {
      "type": "object",
      "properties": { "password": { "$ref": "#/definitions/__schema257" } },
      "required": ["password"],
      "additionalProperties": false,
      "markdownDescription": "Salamander obfuscation from Hysteria2.\n\n`password` is the obfuscation password.\n"
    },
    "__schema257": {
      "type": "string",
      "markdownDescription": "Password for Sudoku and related camouflage settings.\n"
    },
    "__schema258": {
      "type": "string",
      "const": "xdns",
      "markdownDescription": "UDP FinalMask layer type.\n\nSupported values are `header-custom`, `header-dns`, `header-dtls`, `header-srtp`, `header-utp`, `header-wechat`, `header-wireguard`, `mkcp-original`, `mkcp-aes128gcm`, `noise`, `salamander`, `sudoku`, `xdns`, and `xicmp`.\n"
    },
    "__schema259": {
      "type": "object",
      "properties": { "domain": { "$ref": "#/definitions/__schema260" } },
      "required": ["domain"],
      "additionalProperties": false,
      "markdownDescription": "Transmits data through DNS queries in a way similar to DNSTT.\n\n`domain` is the DNS name used for the tunnel.\n"
    },
    "__schema260": {
      "type": "string",
      "markdownDescription": "Domain name used by the camouflage layer.\n"
    },
    "__schema261": {
      "type": "string",
      "const": "xicmp",
      "markdownDescription": "UDP FinalMask layer type.\n\nSupported values are `header-custom`, `header-dns`, `header-dtls`, `header-srtp`, `header-utp`, `header-wechat`, `header-wireguard`, `mkcp-original`, `mkcp-aes128gcm`, `noise`, `salamander`, `sudoku`, `xdns`, and `xicmp`.\n"
    },
    "__schema262": {
      "type": "object",
      "properties": {
        "listenIp": { "$ref": "#/definitions/__schema263" },
        "id": { "$ref": "#/definitions/__schema265" }
      },
      "required": ["id"],
      "additionalProperties": false,
      "markdownDescription": "ICMP camouflage layer.\n\nFields:\n\n- `listenIp`: the IP address to listen on. Defaults to `0.0.0.0`.\n- `id`: when multiple clients share the same IP, keep this value at `0`.\n"
    },
    "__schema263": {
      "markdownDescription": "IP address to listen on. Defaults to `0.0.0.0`.\n\nUnlike normal TCP and UDP listening, this value is specific to ICMP over IPv4 or IPv6.\n",
      "allOf": [{ "$ref": "#/definitions/__schema264" }]
    },
    "__schema264": { "default": "0.0.0.0", "type": "string" },
    "__schema265": {
      "default": 0,
      "markdownDescription": "Identifier used when multiple clients share the same IP.\n\nIt is recommended to keep this value at `0`.\n",
      "allOf": [{ "$ref": "#/definitions/__schema266" }]
    },
    "__schema266": { "type": "integer", "minimum": -9007199254740991, "maximum": 9007199254740991 },
    "__schema267": {
      "markdownDescription": "`FinalMaskObject` corresponds to the `finalmask` item in `StreamSettingsObject`.\n\n[Documentation ↗](https://xtls.github.io/en/config/transports/finalmask.html)\n",
      "allOf": [{ "$ref": "#/definitions/__schema268" }]
    },
    "__schema268": {
      "type": "object",
      "properties": {
        "congestion": { "$ref": "#/definitions/__schema269" },
        "debug": { "$ref": "#/definitions/__schema270" },
        "brutalUp": { "$ref": "#/definitions/__schema272" },
        "brutalDown": { "$ref": "#/definitions/__schema274" },
        "udpHop": { "$ref": "#/definitions/__schema276" },
        "initStreamReceiveWindow": { "$ref": "#/definitions/__schema280" },
        "maxStreamReceiveWindow": { "$ref": "#/definitions/__schema282" },
        "initConnectionReceiveWindow": { "$ref": "#/definitions/__schema284" },
        "maxConnectionReceiveWindow": { "$ref": "#/definitions/__schema286" },
        "maxIdleTimeout": { "$ref": "#/definitions/__schema288" },
        "keepAlivePeriod": { "$ref": "#/definitions/__schema290" },
        "disablePathMTUDiscovery": { "$ref": "#/definitions/__schema292" },
        "maxIncomingStreams": { "$ref": "#/definitions/__schema294" }
      },
      "required": ["congestion", "udpHop"],
      "additionalProperties": false,
      "markdownDescription": "Used for QUIC parameter tuning in XHTTP H3 and Hysteria.\n\nFields:\n\n- `congestion`: congestion-control algorithm. Supported values are `reno`, `bbr`, `brutal`, and `force-brutal`.\n- `debug`: enable logging for the `bbr` and `brutal` congestion-control implementations.\n- `brutalUp`: upload rate limit. The default is `0`.\n- `brutalDown`: download rate limit. The default is `0`.\n- `udpHop`: UDP port-hopping configuration.\n- `initStreamReceiveWindow`: low-level QUIC stream receive window.\n- `maxStreamReceiveWindow`: low-level QUIC stream receive window.\n- `initConnectionReceiveWindow`: low-level QUIC connection receive window.\n- `maxConnectionReceiveWindow`: low-level QUIC connection receive window.\n- `maxIdleTimeout`: maximum idle timeout in seconds.\n- `keepAlivePeriod`: QUIC KeepAlive interval in seconds.\n- `disablePathMTUDiscovery`: whether to disable path MTU discovery.\n- `maxIncomingStreams`: server-side maximum incoming streams.\n"
    },
    "__schema269": {
      "type": "string",
      "enum": ["reno", "bbr", "brutal", "force-brutal"],
      "markdownDescription": "Congestion-control algorithm. Supported values are `reno`, `bbr`, `brutal`, and `force-brutal`.\n"
    },
    "__schema270": {
      "markdownDescription": "Enable logging for the `bbr` and `brutal` congestion-control implementations.\n",
      "allOf": [{ "$ref": "#/definitions/__schema271" }]
    },
    "__schema271": { "default": false, "type": "boolean" },
    "__schema272": {
      "markdownDescription": "Upload rate limit. The default value is `0`.\n\nThe format supports common bit-rate forms such as `1000000`, `100kb`, `20 mb`, `100 mbps`, `1g`, and `1 tbps`.\n",
      "allOf": [{ "$ref": "#/definitions/__schema273" }]
    },
    "__schema273": {
      "default": 0,
      "anyOf": [
        { "type": "string" },
        { "type": "integer", "minimum": -9007199254740991, "maximum": 9007199254740991 }
      ]
    },
    "__schema274": {
      "markdownDescription": "Download rate limit. The default value is `0`.\n\nThe format supports common bit-rate forms such as `1000000`, `100kb`, `20 mb`, `100 mbps`, `1g`, and `1 tbps`.\n",
      "allOf": [{ "$ref": "#/definitions/__schema275" }]
    },
    "__schema275": {
      "default": 0,
      "anyOf": [
        { "type": "string" },
        { "type": "integer", "minimum": -9007199254740991, "maximum": 9007199254740991 }
      ]
    },
    "__schema276": {
      "type": "object",
      "properties": {
        "ports": { "$ref": "#/definitions/__schema277" },
        "interval": { "$ref": "#/definitions/__schema278" }
      },
      "required": ["ports"],
      "additionalProperties": false,
      "markdownDescription": "UDP port-hopping configuration.\n\n`ports` specifies the hopping range. It can be a single numeric string like `\"1234\"`, or a range like `\"1145-1919\"`, which means ports 1145 through 1919. Commas can be used to combine ranges, for example `11,13,15-17`.\n\n`interval` is the port-hopping interval in seconds. The minimum is 5. The default is 30 seconds.\n"
    },
    "__schema277": {
      "type": "string",
      "markdownDescription": "UDP port range for hopping. It can be a single numeric string like `\"1234\"`, or a range like `\"1145-1919\"`. Commas can be used to combine ranges.\n"
    },
    "__schema278": {
      "markdownDescription": "UDP port-hopping interval in seconds. The minimum is `5`. The default is `30` seconds.\n",
      "allOf": [{ "$ref": "#/definitions/__schema279" }]
    },
    "__schema279": { "default": 30, "anyOf": [{ "type": "number" }, { "type": "string" }] },
    "__schema280": {
      "markdownDescription": "Low-level QUIC initial stream receive window. Do not change it unless you fully understand what you are doing.\n",
      "allOf": [{ "$ref": "#/definitions/__schema281" }]
    },
    "__schema281": {
      "default": 8388608,
      "type": "integer",
      "minimum": -9007199254740991,
      "maximum": 9007199254740991
    },
    "__schema282": {
      "markdownDescription": "Low-level QUIC maximum stream receive window. Do not change it unless you fully understand what you are doing.\n",
      "allOf": [{ "$ref": "#/definitions/__schema283" }]
    },
    "__schema283": {
      "default": 8388608,
      "type": "integer",
      "minimum": -9007199254740991,
      "maximum": 9007199254740991
    },
    "__schema284": {
      "markdownDescription": "Low-level QUIC initial connection receive window. Do not change it unless you fully understand what you are doing.\n",
      "allOf": [{ "$ref": "#/definitions/__schema285" }]
    },
    "__schema285": {
      "default": 20971520,
      "type": "integer",
      "minimum": -9007199254740991,
      "maximum": 9007199254740991
    },
    "__schema286": {
      "markdownDescription": "Low-level QUIC maximum connection receive window. Do not change it unless you fully understand what you are doing.\n",
      "allOf": [{ "$ref": "#/definitions/__schema287" }]
    },
    "__schema287": {
      "default": 20971520,
      "type": "integer",
      "minimum": -9007199254740991,
      "maximum": 9007199254740991
    },
    "__schema288": {
      "markdownDescription": "Maximum idle timeout in seconds. The supported range is 4 to 120 seconds. The default is 30 seconds.\n",
      "allOf": [{ "$ref": "#/definitions/__schema289" }]
    },
    "__schema289": {
      "default": 30,
      "type": "integer",
      "minimum": -9007199254740991,
      "maximum": 9007199254740991
    },
    "__schema290": {
      "markdownDescription": "QUIC KeepAlive interval in seconds. The supported range is 2 to 60 seconds. Disabled by default.\n",
      "allOf": [{ "$ref": "#/definitions/__schema291" }]
    },
    "__schema291": {
      "default": 0,
      "type": "integer",
      "minimum": -9007199254740991,
      "maximum": 9007199254740991
    },
    "__schema292": {
      "markdownDescription": "Whether to disable path MTU discovery.\n\nIf your operating system is outside Linux, Windows, and Darwin, you may need to disable it manually.\n",
      "allOf": [{ "$ref": "#/definitions/__schema293" }]
    },
    "__schema293": { "default": false, "type": "boolean" },
    "__schema294": {
      "markdownDescription": "Server-side only. If set, it must not be smaller than `8`.\n",
      "allOf": [{ "$ref": "#/definitions/__schema295" }]
    },
    "__schema295": {
      "default": 1024,
      "type": "integer",
      "minimum": 8,
      "maximum": 9007199254740991
    },
    "__schema296": { "allOf": [{ "$ref": "#/definitions/__schema297" }] },
    "__schema297": {
      "type": "object",
      "properties": {
        "mark": { "$ref": "#/definitions/__schema298" },
        "tcpMaxSeg": { "$ref": "#/definitions/__schema300" },
        "tcpFastOpen": { "$ref": "#/definitions/__schema302" },
        "tproxy": { "$ref": "#/definitions/__schema304" },
        "domainStrategy": { "$ref": "#/definitions/__schema306" },
        "happyEyeballs": { "$ref": "#/definitions/__schema308" },
        "dialerProxy": { "$ref": "#/definitions/__schema318" },
        "acceptProxyProtocol": { "$ref": "#/definitions/__schema320" },
        "tcpKeepAliveInterval": { "$ref": "#/definitions/__schema322" },
        "tcpKeepAliveIdle": { "$ref": "#/definitions/__schema324" },
        "tcpUserTimeout": { "$ref": "#/definitions/__schema326" },
        "tcpcongestion": { "$ref": "#/definitions/__schema328" },
        "interface": { "$ref": "#/definitions/__schema330" },
        "V6Only": { "$ref": "#/definitions/__schema332" },
        "tcpWindowClamp": { "$ref": "#/definitions/__schema334" },
        "tcpMptcp": { "$ref": "#/definitions/__schema336" },
        "addressPortStrategy": { "$ref": "#/definitions/__schema338" },
        "customSockopt": { "$ref": "#/definitions/__schema340" }
      },
      "additionalProperties": false,
      "markdownDescription": "Sockopt is used to configure low-level network behavior.\n\nIt can be used to tune transparent proxying, DNS resolution strategy, and many other socket-level options.\n\n[Documentation ↗](https://xtls.github.io/en/config/transports/sockopt.html)\n"
    },
    "__schema298": {
      "markdownDescription": "An integer. When non-zero, outbound connections are marked with this value through `SO_MARK`.\n\n- Linux only.\n- Requires `CAP_NET_ADMIN`.\n",
      "allOf": [{ "$ref": "#/definitions/__schema299" }]
    },
    "__schema299": { "type": "integer", "minimum": -9007199254740991, "maximum": 9007199254740991 },
    "__schema300": {
      "markdownDescription": "Used to set the maximum segment size of TCP packets.\n",
      "allOf": [{ "$ref": "#/definitions/__schema301" }]
    },
    "__schema301": { "type": "integer", "minimum": -9007199254740991, "maximum": 9007199254740991 },
    "__schema302": {
      "markdownDescription": "Whether to enable TCP Fast Open.\n\nWhen set to `true` or a positive integer, TFO is enabled. When set to `false` or a negative value, TFO is forcibly disabled. When the field is absent or `0`, the system default is used. This option is available for both inbound and outbound.\n",
      "allOf": [{ "$ref": "#/definitions/__schema303" }]
    },
    "__schema303": { "anyOf": [{ "type": "boolean" }, { "type": "number" }] },
    "__schema304": {
      "markdownDescription": "Whether to enable transparent proxying. Linux only.\n\n- `\"redirect\"`: transparent proxy in Redirect mode, supporting all IPv4 and IPv6 TCP connections\n- `\"tproxy\"`: transparent proxy in TProxy mode, supporting all IPv4 and IPv6 TCP and UDP connections\n- `\"off\"`: disable transparent proxying\n\nTransparent proxying requires root or `CAP_NET_ADMIN`.\n\n### DANGER\n\nWhen `Dokodemo-door` has `followRedirect` set to `true`, and `tproxy` is empty in Sockopt, the `tproxy` value is set to `\"redirect\"`.\n",
      "allOf": [{ "$ref": "#/definitions/__schema305" }]
    },
    "__schema305": { "default": "off", "type": "string", "enum": ["redirect", "tproxy", "off"] },
    "__schema306": {
      "markdownDescription": "When the target address is a domain name, this field controls how outbound connections resolve and use that target.\n\nThe default value is `\"AsIs\"`.\n\n- `\"AsIs\"`: Xray does not specially handle the domain name. In the end it uses Go's built-in dialer directly.\n- Any other value: Xray uses the built-in DNS server for resolution. If there is no `DNSObject`, system DNS is used. If multiple IP addresses match, the core randomly picks one target IP.\n- `\"IPv4\"` means try IPv4 only. `\"IPv4v6\"` means try IPv4 or IPv6, but for dual-stack domains prefer IPv4. The same logic applies to the IPv6-first variants.\n\nWhen built-in DNS also sets `queryStrategy`, the actual behavior is the intersection of the two settings.\n\n### DANGER\n\nImproper use of this feature can create an infinite loop when routing DNS traffic through the proxy itself.\n",
      "allOf": [{ "$ref": "#/definitions/__schema307" }]
    },
    "__schema307": {
      "default": "AsIs",
      "type": "string",
      "enum": [
        "AsIs",
        "UseIP",
        "UseIPv6v4",
        "UseIPv6",
        "UseIPv4v6",
        "UseIPv4",
        "ForceIP",
        "ForceIPv6v4",
        "ForceIPv6",
        "ForceIPv4v6",
        "ForceIPv4"
      ]
    },
    "__schema308": { "allOf": [{ "$ref": "#/definitions/__schema309" }] },
    "__schema309": {
      "default": {},
      "type": "object",
      "properties": {
        "tryDelayMs": { "$ref": "#/definitions/__schema310" },
        "prioritizeIPv6": { "$ref": "#/definitions/__schema312" },
        "interleave": { "$ref": "#/definitions/__schema314" },
        "maxConcurrentTry": { "$ref": "#/definitions/__schema316" }
      },
      "additionalProperties": false,
      "markdownDescription": "An RFC 8305 Happy Eyeballs implementation for TCP connections.\n\nWhen the target is a domain name, it races the resolved addresses and chooses the first successful one. It only works when `Sockopt.domainStrategy` is not `AsIs`.\n\n### WARNING\n\nDo not use this together with the `domainStrategy` on a `Freedom` outbound, because then `Sockopt` only sees the final IP after replacement.\n"
    },
    "__schema310": {
      "markdownDescription": "Delay between each racing attempt, in milliseconds. The default is `0`, which disables the feature. A recommended value is `250`.\n",
      "allOf": [{ "$ref": "#/definitions/__schema311" }]
    },
    "__schema311": {
      "default": 0,
      "type": "integer",
      "minimum": -9007199254740991,
      "maximum": 9007199254740991
    },
    "__schema312": {
      "markdownDescription": "Controls which IP family comes first after sorting. The default is `false`, meaning IPv4 comes first.\n",
      "allOf": [{ "$ref": "#/definitions/__schema313" }]
    },
    "__schema313": { "default": false, "type": "boolean" },
    "__schema314": {
      "markdownDescription": "The RFC 8305 `First Address Family Count`. The default value is `1`. It controls how IPv4 and IPv6 addresses are interleaved.\n\nFor example, the waiting dial queue may be ordered as `46464646` when this is `1`, or `44664466` when it is `2`, where `6` means an IPv6 address and `4` means an IPv4 address.\n",
      "allOf": [{ "$ref": "#/definitions/__schema315" }]
    },
    "__schema315": {
      "default": 1,
      "type": "integer",
      "minimum": -9007199254740991,
      "maximum": 9007199254740991
    },
    "__schema316": {
      "markdownDescription": "Maximum number of concurrent attempts. This prevents the core from launching too many connections when a domain resolves to many addresses and all of them fail.\n\nThe default is `4`. Setting it to `0` disables Happy Eyeballs.\n",
      "allOf": [{ "$ref": "#/definitions/__schema317" }]
    },
    "__schema317": {
      "default": 4,
      "type": "integer",
      "minimum": -9007199254740991,
      "maximum": 9007199254740991
    },
    "__schema318": {
      "markdownDescription": "An outbound identifier. When non-empty, the specified outbound is used to establish the connection. It can be used for chained forwarding that still respects transport configuration.\n\n### DANGER\n\nThis option is incompatible with `ProxySettingsObject.Tag`.\n",
      "allOf": [{ "$ref": "#/definitions/__schema319" }]
    },
    "__schema319": { "type": "string" },
    "__schema320": {
      "markdownDescription": "Inbound-only. Controls whether PROXY protocol is accepted.\n\nIf you do not know what it is, ignore this option.\n\nWhen set to `true`, the peer must send PROXY protocol v1 or v2 immediately after the underlying TCP connection is established, otherwise the connection is closed.\n",
      "allOf": [{ "$ref": "#/definitions/__schema321" }]
    },
    "__schema321": { "default": false, "type": "boolean" },
    "__schema322": {
      "markdownDescription": "Time interval in seconds between Keep-Alive probes after TCP enters Keep-Alive state. The rest of the behavior is described in `tcpKeepAliveIdle`.\n",
      "allOf": [{ "$ref": "#/definitions/__schema323" }]
    },
    "__schema323": { "type": "integer", "minimum": -9007199254740991, "maximum": 9007199254740991 },
    "__schema324": {
      "markdownDescription": "TCP idle threshold in seconds. Once a TCP connection has been idle for this long, Keep-Alive probes begin.\n\nFor outbound, Xray uses Chrome's default values, where both idle and interval are 45 seconds. Setting either this field or `tcpKeepAliveInterval` to a negative value disables that default keepalive; a positive value overrides it.\n\nFor inbound, Keep-Alive is disabled by default. It becomes enabled when either this field or `tcpKeepAliveInterval` is non-zero. If only one is set, the other follows the operating-system default.\n",
      "allOf": [{ "$ref": "#/definitions/__schema325" }]
    },
    "__schema325": { "type": "integer", "minimum": -9007199254740991, "maximum": 9007199254740991 },
    "__schema326": {
      "markdownDescription": "TCP user timeout, in milliseconds.\n\nSee the gRPC proposal for the underlying socket option behavior.\n",
      "allOf": [{ "$ref": "#/definitions/__schema327" }]
    },
    "__schema327": { "type": "integer", "minimum": -9007199254740991, "maximum": 9007199254740991 },
    "__schema328": {
      "markdownDescription": "TCP congestion-control algorithm. Linux only. When unset, the operating-system default is used.\n\nCommon algorithms include `bbr` (recommended), `cubic`, and `reno`.\n",
      "allOf": [{ "$ref": "#/definitions/__schema329" }]
    },
    "__schema329": { "type": "string", "enum": ["bbr", "cubic", "reno", ""] },
    "__schema330": {
      "markdownDescription": "Bind the outbound connection to a specific network-interface name. Supported on Linux, iOS, macOS, and Windows.\n",
      "allOf": [{ "$ref": "#/definitions/__schema331" }]
    },
    "__schema331": { "type": "string" },
    "__schema332": {
      "markdownDescription": "When set to `true`, listening on `::` accepts only IPv6 connections. Linux only.\n",
      "allOf": [{ "$ref": "#/definitions/__schema333" }]
    },
    "__schema333": { "default": false, "type": "boolean" },
    "__schema334": {
      "markdownDescription": "Bind the advertised TCP window size to this value. The kernel chooses the larger value between this and `SOCK_MIN_RCVBUF / 2`.\n",
      "allOf": [{ "$ref": "#/definitions/__schema335" }]
    },
    "__schema335": { "type": "integer", "minimum": -9007199254740991, "maximum": 9007199254740991 },
    "__schema336": {
      "markdownDescription": "When set to `true`, Multipath TCP is enabled. This is client-only, because starting with Go 1.24 MPTCP is enabled by default when listening. It currently requires Linux kernel 5.6 or later.\n",
      "allOf": [{ "$ref": "#/definitions/__schema337" }]
    },
    "__schema337": { "default": false, "type": "boolean" },
    "__schema338": {
      "markdownDescription": "Use SRV records or TXT records to specify the target address and or port used by outbound. The default value is `none`, which disables the feature.\n\nThese lookups go through system DNS rather than Xray's built-in DNS. The queried name is the outbound domain name. If the lookup fails, the request is sent using the original address and port.\n\n`Srv*` means querying SRV records in their standard format. `Txt*` means querying TXT records in a format such as `127.0.0.1:80`.\n\n`PortOnly` resets only the port. `AddressOnly` resets only the address. `PortAndAddress` resets both.\n",
      "allOf": [{ "$ref": "#/definitions/__schema339" }]
    },
    "__schema339": {
      "default": "none",
      "type": "string",
      "enum": [
        "none",
        "SrvPortOnly",
        "SrvAddressOnly",
        "SrvPortAndAddress",
        "TxtPortOnly",
        "TxtAddressOnly",
        "TxtPortAndAddress"
      ]
    },
    "__schema340": {
      "markdownDescription": "An array for advanced users to specify any needed socket option.\n\nIn theory, all connection-related settings above can be expressed here, and you can also set other socket options that exist but are not exposed directly by the core. It currently supports Linux, Windows, and Darwin.\n\n### WARNING\n\nMake sure you understand socket programming before using it.\n\n```json\n[\n  {\n    \"system\": \"linux\",\n    \"type\": \"str\",\n    \"level\": \"6\",\n    \"opt\": \"13\",\n    \"value\": \"bbr\"\n  }\n]\n```\n",
      "allOf": [{ "$ref": "#/definitions/__schema341" }]
    },
    "__schema341": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "system": { "$ref": "#/definitions/__schema342" },
          "type": { "$ref": "#/definitions/__schema344" },
          "level": { "$ref": "#/definitions/__schema345" },
          "opt": { "$ref": "#/definitions/__schema347" },
          "value": { "$ref": "#/definitions/__schema350" }
        },
        "required": ["type", "opt", "value"],
        "additionalProperties": false,
        "markdownDescription": "An array for advanced users to specify any needed socket option.\n\nIn theory, all connection-related settings above can be expressed here, and you can also set other socket options that exist but are not exposed directly by the core. It currently supports Linux, Windows, and Darwin.\n\n### WARNING\n\nMake sure you understand socket programming before using it.\n\n```json\n[\n  {\n    \"system\": \"linux\",\n    \"type\": \"str\",\n    \"level\": \"6\",\n    \"opt\": \"13\",\n    \"value\": \"bbr\"\n  }\n]\n```\n"
      }
    },
    "__schema342": {
      "markdownDescription": "Optional. Restricts the option to a specific operating system. If the current system does not match, the option is skipped. Supported values are `linux`, `windows`, and `darwin`, all in lowercase. If left empty, the option is applied directly.\n",
      "allOf": [{ "$ref": "#/definitions/__schema343" }]
    },
    "__schema343": {
      "anyOf": [
        { "type": "string", "enum": ["linux", "windows", "darwin", ""] },
        { "type": "string" }
      ]
    },
    "__schema344": {
      "type": "string",
      "enum": ["int", "str"],
      "markdownDescription": "Required. The value type to set. Currently `int` and `str` are supported.\n"
    },
    "__schema345": {
      "markdownDescription": "Optional. Protocol level. The default is `6`, which means TCP.\n",
      "allOf": [{ "$ref": "#/definitions/__schema346" }]
    },
    "__schema346": {
      "default": "6",
      "anyOf": [
        { "type": "string" },
        { "type": "integer", "minimum": -9007199254740991, "maximum": 9007199254740991 }
      ]
    },
    "__schema347": {
      "anyOf": [{ "$ref": "#/definitions/__schema348" }, { "$ref": "#/definitions/__schema349" }],
      "markdownDescription": "The option number to operate on, in decimal. In the example above, `13` is the decimal form of `TCP_CONGESTION`, whose hexadecimal value is `0xd`.\n"
    },
    "__schema348": { "type": "string" },
    "__schema349": { "type": "integer", "minimum": -9007199254740991, "maximum": 9007199254740991 },
    "__schema350": {
      "anyOf": [{ "$ref": "#/definitions/__schema351" }, { "$ref": "#/definitions/__schema352" }],
      "markdownDescription": "The value to set. In the example above, the value is `bbr`.\n\nWhen `type` is `int`, the value must be a decimal number.\n"
    },
    "__schema351": { "type": "string" },
    "__schema352": { "type": "integer", "minimum": -9007199254740991, "maximum": 9007199254740991 },
    "__schema353": { "type": "string", "const": "tls" },
    "__schema354": {
      "type": "object",
      "properties": {
        "serverName": { "$ref": "#/definitions/__schema355" },
        "verifyPeerCertByName": { "$ref": "#/definitions/__schema357" },
        "rejectUnknownSni": { "$ref": "#/definitions/__schema359" },
        "allowInsecure": { "$ref": "#/definitions/__schema361" },
        "alpn": { "$ref": "#/definitions/__schema363" },
        "minVersion": { "$ref": "#/definitions/__schema365" },
        "maxVersion": { "$ref": "#/definitions/__schema367" },
        "cipherSuites": { "$ref": "#/definitions/__schema369" },
        "certificates": { "$ref": "#/definitions/__schema371" },
        "disableSystemRoot": { "$ref": "#/definitions/__schema389" },
        "enableSessionResumption": { "$ref": "#/definitions/__schema391" },
        "fingerprint": { "$ref": "#/definitions/__schema393" },
        "pinnedPeerCertSha256": { "$ref": "#/definitions/__schema394" },
        "curvePreferences": { "$ref": "#/definitions/__schema396" },
        "masterKeyLog": { "$ref": "#/definitions/__schema398" },
        "echServerKeys": { "$ref": "#/definitions/__schema400" },
        "echConfigList": { "$ref": "#/definitions/__schema402" },
        "echSockopt": { "$ref": "#/definitions/__schema404" }
      },
      "additionalProperties": false,
      "markdownDescription": "`TLSObject` corresponds to the `tlsSettings` item in `StreamSettingsObject`.\n\n[Documentation ↗](https://xtls.github.io/en/config/transports/tls.html)\n"
    },
    "__schema355": {
      "markdownDescription": "Server name. The server certificate's SAN must contain this value. It can be a domain name or an IP address. If it is a domain name, it will be sent in the SNI extension of the Client Hello. IP addresses will not send the SNI extension, because SNI does not allow IP addresses. If you fill in an IPv6 address, wrap it in `[]`.\n\nWhen left empty, the value in `address` is used automatically if that value is a domain name.\n\nSpecial value `\"FromMitM\"` causes Xray to use the SNI extracted from TLS decrypted by a `dokodemo-door` inbound.\n",
      "allOf": [{ "$ref": "#/definitions/__schema356" }]
    },
    "__schema356": {
      "default": "",
      "anyOf": [{ "type": "string" }, { "type": "string", "const": "FromMitM" }]
    },
    "__schema357": {
      "markdownDescription": "Client-only. The SNI used for certificate verification. Multiple domain names can be separated with `,`; it is enough for any one SAN in the certificate to match one of them. This overrides the `serverName` used for verification and is intended for special cases such as domain fronting.\n\nSpecial value `\"FromMitM\"` causes Xray to additionally include the SNI extracted from TLS decrypted by a `dokodemo-door` inbound.\n",
      "allOf": [{ "$ref": "#/definitions/__schema358" }]
    },
    "__schema358": { "anyOf": [{ "type": "string" }, { "type": "string", "const": "FromMitM" }] },
    "__schema359": {
      "markdownDescription": "When set to `true`, the server rejects the TLS handshake if the received SNI does not match the certificate domain. The default is `false`.\n",
      "allOf": [{ "$ref": "#/definitions/__schema360" }]
    },
    "__schema360": { "default": false, "type": "boolean" },
    "__schema361": {
      "deprecated": true,
      "deprecationMessage": "This option is deprecated. Use `pinnedPeerCertSha256` to specify the certificate manually instead.",
      "allOf": [{ "$ref": "#/definitions/__schema362" }]
    },
    "__schema362": { "default": false, "type": "boolean" },
    "__schema363": {
      "markdownDescription": "An array of strings that specifies the ALPN values used during TLS handshake. The default value is `[\"h2\", \"http/1.1\"]`.\n\nSpecial value `[\"FromMitM\"]`, when it is the only element, causes outbound TLS to reuse the ALPN from the TLS connection decrypted by a `dokodemo-door` inbound.\n",
      "allOf": [{ "$ref": "#/definitions/__schema364" }]
    },
    "__schema364": {
      "default": ["h2", "http/1.1"],
      "anyOf": [
        { "type": "array", "items": { "type": "string", "enum": ["http/1.1", "h2", "h3"] } },
        { "type": "string", "const": "FromMitM" }
      ]
    },
    "__schema365": {
      "markdownDescription": "`minVersion` is the minimum acceptable TLS version.\n",
      "allOf": [{ "$ref": "#/definitions/__schema366" }]
    },
    "__schema366": { "type": "string" },
    "__schema367": {
      "markdownDescription": "`maxVersion` is the maximum acceptable TLS version.\n",
      "allOf": [{ "$ref": "#/definitions/__schema368" }]
    },
    "__schema368": { "type": "string" },
    "__schema369": {
      "markdownDescription": "`cipherSuites` configures the list of supported cipher suites, separated by `:`.\n\nYou can find Go cipher-suite names and descriptions in the Go source code.\n\n### DANGER\n\nThese options are not required in normal cases and usually do not affect security. If left unset, Go chooses automatically according to the platform. If you are not familiar with them, do not configure them.\n",
      "allOf": [{ "$ref": "#/definitions/__schema370" }]
    },
    "__schema370": { "type": "string" },
    "__schema371": {
      "markdownDescription": "Certificate list. Each item represents one certificate. A full chain is recommended.\n\n### TIP\n\nIf you want an A or A+ rating from tools such as ssllibs or myssl, a full chain certificate is recommended.\n",
      "allOf": [{ "$ref": "#/definitions/__schema372" }]
    },
    "__schema372": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "ocspStapling": { "$ref": "#/definitions/__schema373" },
          "oneTimeLoading": { "$ref": "#/definitions/__schema375" },
          "usage": { "$ref": "#/definitions/__schema377" },
          "buildChain": { "$ref": "#/definitions/__schema379" },
          "certificateFile": { "$ref": "#/definitions/__schema381" },
          "keyFile": { "$ref": "#/definitions/__schema383" },
          "certificate": { "$ref": "#/definitions/__schema385" },
          "key": { "$ref": "#/definitions/__schema387" }
        },
        "additionalProperties": false,
        "markdownDescription": "`CertificateObject` represents a certificate and key pair used by TLS.\n\nServer certificates are hot-reloaded every 3600 seconds (once per hour).\n\n[Documentation ↗](https://xtls.github.io/en/config/transports/tls.html#certificateobject)\n"
      }
    },
    "__schema373": {
      "markdownDescription": "OCSP stapling refresh interval in seconds. The default value is `0`. Any non-zero value enables OCSP stapling and also replaces the default 3600-second certificate hot-reload interval; reloading and OCSP stapling happen together.\n",
      "allOf": [{ "$ref": "#/definitions/__schema374" }]
    },
    "__schema374": { "default": 0, "type": "integer", "minimum": 0, "maximum": 9007199254740991 },
    "__schema375": {
      "markdownDescription": "Load only once. The default is `false`. When set to `true`, both certificate hot reload and OCSP stapling are disabled.\n",
      "allOf": [{ "$ref": "#/definitions/__schema376" }]
    },
    "__schema376": { "default": false, "type": "boolean" },
    "__schema377": {
      "markdownDescription": "Certificate usage. The default value is `\"encipherment\"`.\n\n- `\"encipherment\"`: the certificate is used for TLS authentication and encryption\n- `\"verify\"`: the certificate is used to verify remote TLS certificates; in this case it must be a CA certificate\n- `\"issue\"`: the certificate is used to issue other certificates; in this case it must also be a CA certificate\n\n### TIP\n\nOn Windows, you can install a self-signed CA certificate into the system store to verify remote TLS certificates.\n\n### TIP\n\nWhen a new client request arrives, suppose the specified `serverName` is `\"xray.com\"`. Xray first looks through the certificate list for a certificate usable for `\"xray.com\"`. If none is found, it uses any certificate whose `usage` is `\"issue\"` to issue one suitable for `\"xray.com\"` with a one-hour validity period, then adds that new certificate to the list for later use.\n\n### TIP\n\nYou can generate a self-signed CA certificate with `xray tls cert`.\n\n### TIP\n\nIf you already own a domain name, you can conveniently obtain free third-party certificates with tools such as [acme.sh](https://github.com/acmesh-official/acme.sh).\n",
      "allOf": [{ "$ref": "#/definitions/__schema378" }]
    },
    "__schema378": {
      "default": "encipherment",
      "type": "string",
      "enum": ["encipherment", "verify", "issue"]
    },
    "__schema379": {
      "markdownDescription": "Only takes effect when the certificate usage is `issue`. If set to `true`, the CA certificate is embedded into the issued certificate chain.\n\n### TIP\n\nYou should not embed a root certificate into the chain. This option is suitable only when the signing CA certificate is an intermediate certificate.\n",
      "allOf": [{ "$ref": "#/definitions/__schema380" }]
    },
    "__schema380": { "default": false, "type": "boolean" },
    "__schema381": {
      "markdownDescription": "Path to the certificate file, for example a `.crt` file generated by OpenSSL.\n\n### TIP\n\nWhen both `certificateFile` and `certificate` are specified, Xray prefers `certificateFile`.\n",
      "allOf": [{ "$ref": "#/definitions/__schema382" }]
    },
    "__schema382": { "type": "string" },
    "__schema383": {
      "markdownDescription": "Path to the private-key file, for example a `.key` file generated by OpenSSL. Password-protected key files are not currently supported.\n\n### TIP\n\nWhen both `keyFile` and `key` are specified, Xray prefers `keyFile`.\n",
      "allOf": [{ "$ref": "#/definitions/__schema384" }]
    },
    "__schema384": { "type": "string" },
    "__schema385": {
      "markdownDescription": "An array of strings representing the certificate content, in the same format as PEM-encoded certificates. Choose either `certificate` or `certificateFile`.\n",
      "allOf": [{ "$ref": "#/definitions/__schema386" }]
    },
    "__schema386": { "type": "array", "items": { "type": "string" } },
    "__schema387": {
      "markdownDescription": "An array of strings representing the private-key content, in the same format as PEM-encoded private keys. Choose either `key` or `keyFile`.\n\n### TIP\n\nWhen `usage` is `\"verify\"`, both `keyFile` and `key` may be empty.\n",
      "allOf": [{ "$ref": "#/definitions/__schema388" }]
    },
    "__schema388": { "type": "array", "items": { "type": "string" } },
    "__schema389": {
      "markdownDescription": "Whether to disable the operating system's built-in CA certificates. The default value is `false`.\n\nWhen set to `true`, Xray uses only the certificates specified in `certificates` during TLS handshake. When set to `false`, Xray uses only the operating system's built-in CA certificates during TLS handshake.\n",
      "allOf": [{ "$ref": "#/definitions/__schema390" }]
    },
    "__schema390": { "default": false, "type": "boolean" },
    "__schema391": {
      "markdownDescription": "Whether to enable session resumption. It is disabled by default, and session resumption is only attempted when both the server and the client enable it.\n\nIf negotiation succeeds, certificates do not need to be transmitted during the handshake. This saves a tiny amount of handshake time, which is usually negligible.\n\nNote that this is not TLS 0-RTT. `gotls` does not support that feature yet, so this does not reduce TLS handshake RTT.\n",
      "allOf": [{ "$ref": "#/definitions/__schema392" }]
    },
    "__schema392": { "default": false, "type": "boolean" },
    "__schema393": {
      "markdownDescription": "This parameter configures the fingerprint of the `TLS Client Hello`. The default value is `chrome`. To revert to native Go TLS, set it to `unsafe`. When enabled, Xray uses the uTLS library to simulate a TLS fingerprint, or generates one randomly. Three configuration styles are supported:\n\n1. TLS fingerprints of the latest versions of common browsers:\n   - `\"chrome\"`, `\"firefox\"`, `\"safari\"`, `\"ios\"`, `\"android\"`, `\"edge\"`, `\"360\"`, `\"qq\"`\n\n2. Automatically generate a fingerprint when Xray starts:\n   - `\"random\"`: randomly choose one from newer browser versions\n   - `\"randomized\"`: generate a completely random unique fingerprint that fully supports TLS 1.3 with X25519\n\n3. Use native uTLS hello names such as `\"HelloRandomizedNoALPN\"` or `\"HelloChrome_106_Shuffle\"`. See the full list in the [uTLS library](https://github.com/refraction-networking/utls/blob/master/u_common.go#L434).\n\n### TIP\n\nThis feature only simulates the `TLS Client Hello` fingerprint. Behavior and other fingerprints remain the same as Go. If you want more complete browser-like TLS fingerprints and behavior, use Browser Dialer.\n\n### TIP\n\nWhen this feature is enabled, some TLS options that affect TLS fingerprints are overridden by the uTLS library and stop taking effect, such as ALPN. The parameters still passed through are: `\"serverName\"`, `\"disableSystemRoot\"`, `\"pinnedPeerCertSha256\"`, `\"masterKeyLog\"`.\n",
      "allOf": [{ "$ref": "#/definitions/__schema189" }]
    },
    "__schema394": {
      "markdownDescription": "Used to specify the SHA-256 hash of the remote server certificate. It uses hexadecimal encoding and is case-insensitive, for example `e8e2d387fdbffeb38e9c9065cf30a97ee23c0e3d32ee6f78ffae40966befccc9`. Multiple hash values can be joined with `,`, and verification succeeds if any one of them matches.\n\nThis encoding matches the SHA-256 certificate fingerprint shown by the Chrome certificate viewer and the SHA-256 certificate fingerprint format used on crt.sh.\n\nYou can compute it with `xray tls hash --cert <cert.pem>`, or with `openssl x509 -noout -fingerprint -sha256 -in cert.pem`. `xray tls ping` also prints the SHA-256 hash of the remote certificate.\n\nThis check overrides normal certificate validation. There are two cases:\n\n- If the core finds that the matching hash belongs to a leaf certificate, verification succeeds immediately.\n- If the core finds that the matching hash belongs to a CA certificate, whether root or intermediate, it uses the value in `serverName` to verify that the leaf certificate is signed by that CA.\n",
      "allOf": [{ "$ref": "#/definitions/__schema395" }]
    },
    "__schema395": { "type": "string" },
    "__schema396": {
      "markdownDescription": "An array of strings that specifies the curves supported when performing ECDHE during TLS handshake. Supported values are:\n\n- `CurveP256`\n- `CurveP384`\n- `CurveP521`\n- `X25519`\n- `X25519MLKEM768`\n- `SecP256r1MLKEM768*`\n- `SecP384r1MLKEM1024*`\n\n(*: not supported by uTLS)\n\nAs of Go 1.26, the default includes all curves above. Changing the order does not force either side to prefer a specific curve; the actual curve is negotiated by the key-exchange mechanism itself.\n",
      "allOf": [{ "$ref": "#/definitions/__schema397" }]
    },
    "__schema397": {
      "type": "array",
      "items": {
        "type": "string",
        "enum": [
          "CurveP256",
          "CurveP384",
          "CurveP521",
          "X25519",
          "X25519MLKEM768",
          "SecP256r1MLKEM768*",
          "SecP384r1MLKEM1024*"
        ]
      }
    },
    "__schema398": {
      "markdownDescription": "Path to a `(Pre)-Master-Secret` log file. It can be used by software such as Wireshark to decrypt TLS connections sent by Xray.\n",
      "allOf": [{ "$ref": "#/definitions/__schema399" }]
    },
    "__schema399": { "type": "string" },
    "__schema400": {
      "markdownDescription": "Server-only parameter used to enable Encrypted Client Hello on the server.\n\nUse `xray tls ech --serverName example.com` to generate an ECH Server Key and its corresponding Config. `example.com` is the SNI exposed to the outside when SNI is encrypted, and can be any value. The Server Key includes the ECHConfig. If you lose the client-side Config, you can recover it with `xray tls ech -i \"your server key\"`. You can publish it in a DNS HTTPS record; see the format in RFC 9460.\n\nNote that after ECH is configured, the server still accepts normal non-ECH connections.\n",
      "allOf": [{ "$ref": "#/definitions/__schema401" }]
    },
    "__schema401": { "type": "string" },
    "__schema402": {
      "markdownDescription": "Client-only parameter that configures ECHConfig. A non-empty value means the client enables Encrypted Client Hello. Two formats are supported.\n\nThe first is a fixed ECHConfig string.\n\nThe second is querying a DNS server. For example, when using a CDN, you can dynamically obtain ECHConfig from HTTPS records. If a valid ECH Config is obtained, Xray obeys the TTL returned by the server. The query target is the configured SNI, or the configured server domain name if SNI is empty and the target is a domain name.\n\nThe basic format is `\"udp://1.1.1.1\"`, meaning query ECHConfig through UDP DNS 1.1.1.1. You can also use `\"https://1.1.1.1/dns-query\"` or `h2c://` to query via DoH or h2c. All of these support an explicit port, such as `udp://1.1.1.1:53`. If omitted, the default port is 53 or 443 according to the protocol.\n\nYou can also specify a dedicated domain for the ECHConfig lookup in the form `\"example.com+https://1.1.1.1/dns-query\"`. In that case Xray forcibly uses the ECHConfig from the DNS records of `example.com` for the connection. This is useful if you want to obtain ECHConfig from DNS without exposing that you are querying the target domain's HTTPS record, or when you do not want to publish HTTPS records under that domain.\n",
      "allOf": [{ "$ref": "#/definitions/__schema403" }]
    },
    "__schema403": { "type": "string" },
    "__schema404": {
      "markdownDescription": "Adjusts the underlying socket options of the connection used when querying DNS for ECH records.\n",
      "allOf": [{ "$ref": "#/definitions/__schema297" }]
    },
    "__schema405": { "type": "string", "const": "tcp" },
    "__schema406": {
      "anyOf": [
        { "type": "string", "const": "kcp" },
        { "type": "string", "const": "mkcp" }
      ]
    },
    "__schema407": {
      "markdownDescription": "`KcpObject` corresponds to the `kcpSettings` item in [StreamSettingsObject](https://xtls.github.io/en/config/transport.html#streamsettingsobject).\n\n```json\n{\n  // outbound example; also applies to inbound\n  \"outbounds\": [\n    {\n      // ...\n      \"streamSettings\": {\n        \"network\": \"mkcp\",\n        \"kcpSettings\": {\n          \"mtu\": 1350,\n          \"tti\": 20,\n          \"uplinkCapacity\": 5,\n          \"downlinkCapacity\": 20,\n          \"congestion\": false,\n          \"readBufferSize\": 1,\n          \"writeBufferSize\": 1\n        }\n      }\n    }\n  ]\n}\n```\n\n### TIP\n\nThe `header` and `seed` fields have been removed. Please use [FinalMask](https://xtls.github.io/en/config/transports/finalmask.html#finalmaskobject) for configuration. Additionally, the previously default mKCP obfuscation has also been removed. To connect to a legacy server, you need to configure `mkcp-original` in FinalMask.\n",
      "allOf": [{ "$ref": "#/definitions/__schema408" }]
    },
    "__schema408": {
      "type": "object",
      "properties": {
        "mtu": {
          "markdownDescription": "Maximum Transmission Unit. Please select a value between 576 and 1460.\n\nDefault value is `1350`.\n",
          "allOf": [{ "$ref": "#/definitions/__schema409" }]
        },
        "tti": {
          "markdownDescription": "Transmission Time Interval, in milliseconds (ms). mKCP will send data at this frequency. Please select a value between 10 and 5000.\n\nDefault value is `50`.\n",
          "allOf": [{ "$ref": "#/definitions/__schema410" }]
        },
        "uplinkCapacity": {
          "markdownDescription": "Uplink capacity, i.e., the maximum bandwidth used by the host to send data. The unit is MB/s. Note that it is Byte, not bit. Can be set to 0, representing a very small bandwidth.\n\nDefault value is `5`.\n\n**TIP:**\n\n`uplinkCapacity` and `downlinkCapacity` determine the transmission speed of mKCP. Taking a client sending data as an example, the client's `uplinkCapacity` specifies the speed of sending data, while the server's `downlinkCapacity` specifies the speed of receiving data. The actual speed will be the smaller of the two values.\n\nIt is recommended to set `downlinkCapacity` to a larger value, such as 100, and set `uplinkCapacity` to the actual network speed. When the speed is insufficient, you can gradually increase the value of `uplinkCapacity` until it is about twice the bandwidth.\n",
          "allOf": [{ "$ref": "#/definitions/__schema411" }]
        },
        "downlinkCapacity": {
          "markdownDescription": "Downlink capacity, i.e., the maximum bandwidth used by the host to receive data. The unit is MB/s. Note that it is Byte, not bit. Can be set to 0, representing a very small bandwidth.\n\nDefault value is `20`.\n\n**TIP:**\n\n`uplinkCapacity` and `downlinkCapacity` determine the transmission speed of mKCP. Taking a client sending data as an example, the client's `uplinkCapacity` specifies the speed of sending data, while the server's `downlinkCapacity` specifies the speed of receiving data. The actual speed will be the smaller of the two values.\n\nIt is recommended to set `downlinkCapacity` to a larger value, such as 100, and set `uplinkCapacity` to the actual network speed. When the speed is insufficient, you can gradually increase the value of `uplinkCapacity` until it is about twice the bandwidth.\n",
          "allOf": [{ "$ref": "#/definitions/__schema412" }]
        },
        "congestion": {
          "markdownDescription": "Whether to enable congestion control.\n\nWhen congestion control is enabled, Xray automatically monitors network quality. When packet loss is severe, it automatically reduces throughput; when the network is smooth, it appropriately increases throughput.\n\nDefault value is `false`.\n",
          "allOf": [{ "$ref": "#/definitions/__schema413" }]
        },
        "readBufferSize": {
          "markdownDescription": "The read buffer size for a single connection, in MB.\n\nDefault value is `2`.\n\n**TIP:**\n\n`readBufferSize` and `writeBufferSize` specify the memory size used by a single connection. When high-speed transmission is required, specifying larger `readBufferSize` and `writeBufferSize` will improve speed to a certain extent, but it will also use more memory.\n\nWhen the network speed does not exceed 20MB/s, the default value of 1MB can meet the demand; beyond that, you can appropriately increase the values of `readBufferSize` and `writeBufferSize`, and then manually balance the relationship between speed and memory.\n",
          "allOf": [{ "$ref": "#/definitions/__schema414" }]
        },
        "writeBufferSize": {
          "markdownDescription": "The write buffer size for a single connection, in MB.\n\nDefault value is `2`.\n\n**TIP:**\n\n`readBufferSize` and `writeBufferSize` specify the memory size used by a single connection. When high-speed transmission is required, specifying larger `readBufferSize` and `writeBufferSize` will improve speed to a certain extent, but it will also use more memory.\n\nWhen the network speed does not exceed 20MB/s, the default value of 1MB can meet the demand; beyond that, you can appropriately increase the values of `readBufferSize` and `writeBufferSize`, and then manually balance the relationship between speed and memory.\n",
          "allOf": [{ "$ref": "#/definitions/__schema415" }]
        }
      },
      "additionalProperties": false
    },
    "__schema409": {
      "default": 1350,
      "type": "integer",
      "minimum": 0,
      "maximum": 9007199254740991
    },
    "__schema410": { "default": 50, "type": "integer", "minimum": 0, "maximum": 9007199254740991 },
    "__schema411": { "default": 5, "type": "integer", "minimum": 0, "maximum": 9007199254740991 },
    "__schema412": { "default": 20, "type": "integer", "minimum": 0, "maximum": 9007199254740991 },
    "__schema413": { "default": false, "type": "boolean" },
    "__schema414": { "default": 2, "type": "integer", "minimum": 0, "maximum": 9007199254740991 },
    "__schema415": { "default": 2, "type": "integer", "minimum": 0, "maximum": 9007199254740991 },
    "__schema416": {
      "anyOf": [
        { "type": "string", "const": "websocket" },
        { "type": "string", "const": "ws" }
      ]
    },
    "__schema417": {
      "markdownDescription": "`WebSocketObject` corresponds to the `wsSettings` item in [StreamSettingsObject](https://xtls.github.io/en/config/transport.html#streamsettingsobject).\n\n```json\n{\n  // outbound example; also applies to inbound\n  \"outbounds\": [\n    {\n      // ...\n      \"streamSettings\": {\n        \"network\": \"websocket\",\n        \"wsSettings\": {\n          \"acceptProxyProtocol\": false,\n          \"path\": \"/\",\n          \"host\": \"xray.com\",\n          \"headers\": {\n            \"key\": \"value\"\n          },\n          \"heartbeatPeriod\": 10\n        }\n      }\n    }\n  ]\n}\n```\n",
      "allOf": [{ "$ref": "#/definitions/__schema418" }]
    },
    "__schema418": {
      "type": "object",
      "properties": {
        "acceptProxyProtocol": {
          "markdownDescription": "Only used for inbound, indicating whether to receive PROXY protocol.\n\n[PROXY protocol](https://www.haproxy.org/download/2.2/doc/proxy-protocol.txt) is specifically used to pass the real source IP and port of the request. If you don't understand it, please ignore this item.\n\nCommon reverse proxy software (such as HAProxy, Nginx) can be configured to send it, and VLESS fallbacks xver can also send it.\n\nWhen set to `true`, after the underlying TCP connection is established, the requester must send PROXY protocol v1 or v2 first; otherwise, the connection will be closed.\n",
          "allOf": [{ "$ref": "#/definitions/__schema419" }]
        },
        "path": {
          "markdownDescription": "The HTTP protocol path used by WebSocket. The default value is `\"/\"`.\n\nIf the client path contains the `ed` parameter (e.g., `/mypath?ed=2560`), `Early Data` will be enabled to reduce latency. It uses the `Sec-WebSocket-Protocol` header to carry the first packet data during the upgrade, where the value represents the first packet length threshold. If the length of the first packet exceeds this value, `Early Data` will not be enabled. The recommended value is 2560, and the maximum value is 8192. Excessively large values may cause some compatibility issues. If you encounter compatibility issues, try lowering the threshold.\n",
          "allOf": [{ "$ref": "#/definitions/__schema420" }]
        },
        "host": {
          "markdownDescription": "The host sent in the WebSocket HTTP request. Default value is empty. If the server-side value is empty, the host value sent by the client is not verified.\n\nWhen this value is specified on the server side, or `host` is specified in `headers`, it will verify whether it matches the client request host.\n\nClient priority for sending host: `host` > `headers` > `address`.\n",
          "allOf": [{ "$ref": "#/definitions/__schema421" }]
        },
        "headers": {
          "markdownDescription": "Client only. Custom HTTP headers, key-value pairs. Each key represents the name of an HTTP header, and the corresponding value is a string.\n\nDefault is empty.\n",
          "allOf": [{ "$ref": "#/definitions/__schema422" }]
        },
        "heartbeatPeriod": {
          "markdownDescription": "Specifies a fixed time interval to send a Ping message to keep the connection alive. If not specified or set to 0, no Ping message is sent, which is the current default behavior.\n",
          "allOf": [{ "$ref": "#/definitions/__schema423" }]
        }
      },
      "additionalProperties": false
    },
    "__schema419": { "default": false, "type": "boolean" },
    "__schema420": { "default": "/", "type": "string" },
    "__schema421": { "default": "", "type": "string" },
    "__schema422": {
      "default": {},
      "type": "object",
      "propertyNames": { "type": "string" },
      "additionalProperties": { "type": "string" }
    },
    "__schema423": { "default": 0, "type": "integer", "minimum": 0, "maximum": 9007199254740991 },
    "__schema424": { "type": "string", "const": "grpc" },
    "__schema425": {
      "markdownDescription": "`GRPCObject` corresponds to the `grpcSettings` item in [StreamSettingsObject](https://xtls.github.io/en/config/transport.html#streamsettingsobject).\n\n```json\n{\n  // outbound example; also applies to inbound\n  \"outbounds\": [\n    {\n      // ...\n      \"streamSettings\": {\n        \"network\": \"grpc\",\n        \"grpcSettings\": {\n          \"authority\": \"grpc.example.com\",\n          \"serviceName\": \"name\",\n          \"multiMode\": false,\n          \"user_agent\": \"custom user agent\",\n          \"idle_timeout\": 60,\n          \"health_check_timeout\": 20,\n          \"permit_without_stream\": false,\n          \"initial_windows_size\": 0\n        }\n      }\n    }\n  ]\n}\n```\n",
      "allOf": [{ "$ref": "#/definitions/__schema426" }]
    },
    "__schema426": {
      "type": "object",
      "properties": {
        "authority": {
          "markdownDescription": "A string. Can be used as Host to achieve some other purposes.\n",
          "allOf": [{ "$ref": "#/definitions/__schema427" }]
        },
        "serviceName": {
          "markdownDescription": "A string. Specifies the service name, similar to Path in HTTP/2. The client uses this name for communication, and the server verifies if the service name matches.\n\n### TIP\n\nWhen `serviceName` starts with a slash, you can customize the path. It requires at least two slashes. For example, fill in `\"serviceName\": \"/my/sample/path1|path2\"` on the server side, and the client can fill in `\"serviceName\": \"/my/sample/path1\"` or `\"/my/sample/path2\"`.\n",
          "allOf": [{ "$ref": "#/definitions/__schema428" }]
        },
        "multiMode": {
          "markdownDescription": "`true` enables `multiMode`. Default value: `false`.\n\nThis is an experimental option. It may not be kept long-term and cross-version compatibility is not guaranteed. This mode can bring about a 20% performance improvement in test environments, but actual results vary depending on transfer rates.\n\n### TIP\n\nOnly needs to be configured on outbound (client).\n",
          "allOf": [{ "$ref": "#/definitions/__schema429" }]
        },
        "user_agent": {
          "markdownDescription": "### TIP\n\nOnly needs to be configured on outbound (client).\n\nSet the User-Agent for gRPC. This may prevent some CDNs from blocking gRPC traffic.\n",
          "allOf": [{ "$ref": "#/definitions/__schema430" }]
        },
        "idle_timeout": {
          "markdownDescription": "Unit: seconds. When there is no data transmission during this period, a health check will be performed. If this value is set below `10`, `10` will be used (the minimum value).\n\n### TIP\n\nIf you are not using reverse proxy tools like Caddy or Nginx (usually not), and set this below `60`, the server might send unexpected h2 GOAWAY frames to close existing connections.\n\nHealth checks are disabled by default.\n\n### TIP\n\nOnly needs to be configured on outbound (client).\n\n### TIP\n\nMay resolve some \"disconnection\" issues.\n",
          "allOf": [{ "$ref": "#/definitions/__schema431" }]
        },
        "health_check_timeout": {
          "markdownDescription": "Unit: seconds. The timeout for health checks. If the health check is not completed within this time, and there is still no data transmission, the health check is considered failed. Default value is `20`.\n\n### TIP\n\nOnly needs to be configured on outbound (client).\n",
          "allOf": [{ "$ref": "#/definitions/__schema432" }]
        },
        "permit_without_stream": {
          "markdownDescription": "`true` allows health checks when there are no sub-connections (streams). Default value is `false`.\n\n### TIP\n\nOnly needs to be configured on outbound (client).\n",
          "allOf": [{ "$ref": "#/definitions/__schema433" }]
        },
        "initial_windows_size": {
          "markdownDescription": "Initial window size for h2 Stream. When the value is less than or equal to `0`, this feature does not take effect. When the value is greater than `65535`, the Dynamic Window mechanism will be disabled. Default value is `0` (disabled).\n\n### TIP\n\nOnly needs to be configured on outbound (client).\n\n### TIP\n\nWhen going through Cloudflare CDN, you can set the value to `65536` or higher (disabling Dynamic Window) to prevent Cloudflare CDN from sending unexpected h2 GOAWAY frames to close existing connections.\n",
          "allOf": [{ "$ref": "#/definitions/__schema434" }]
        }
      },
      "required": ["idle_timeout"],
      "additionalProperties": false
    },
    "__schema427": { "type": "string" },
    "__schema428": { "default": "", "type": "string" },
    "__schema429": { "default": false, "type": "boolean" },
    "__schema430": { "type": "string" },
    "__schema431": {},
    "__schema432": { "default": 20, "type": "integer", "minimum": 0, "maximum": 9007199254740991 },
    "__schema433": { "default": false, "type": "boolean" },
    "__schema434": { "default": 0, "type": "integer", "minimum": 0, "maximum": 9007199254740991 },
    "__schema435": { "type": "string", "const": "hysteria" },
    "__schema436": {
      "markdownDescription": "`HysteriaObject` corresponds to the `hysteriaSettings` item in [StreamSettingsObject](https://xtls.github.io/en/config/transport.html#streamsettingsobject).\n",
      "allOf": [{ "$ref": "#/definitions/__schema437" }]
    },
    "__schema437": {
      "type": "object",
      "properties": {
        "version": {
          "default": 2,
          "markdownDescription": "Hysteria version, must be 2.\n",
          "allOf": [{ "$ref": "#/definitions/__schema438" }]
        },
        "auth": {
          "type": "string",
          "markdownDescription": "Hysteria authentication password. Must be consistent between the server and the client.\n\nWhen used with `hysteria inbound`, it will be overridden by `users` (if it exists).\n"
        },
        "udpIdleTimeout": {
          "markdownDescription": "Unit: seconds, default 60.\n\nIdle wait time for a single `quic native udp` connection. If this time is too long, it may not be strictly adhered to and may be terminated by the policy first.\n",
          "allOf": [{ "$ref": "#/definitions/__schema439" }]
        },
        "masquerade": {
          "type": "object",
          "properties": {
            "type": { "$ref": "#/definitions/__schema440" },
            "dir": { "$ref": "#/definitions/__schema442" },
            "url": { "$ref": "#/definitions/__schema444" },
            "rewriteHost": { "$ref": "#/definitions/__schema446" },
            "insecure": { "$ref": "#/definitions/__schema448" },
            "content": { "$ref": "#/definitions/__schema450" },
            "headers": { "$ref": "#/definitions/__schema452" },
            "statusCode": { "$ref": "#/definitions/__schema454" }
          },
          "additionalProperties": false,
          "markdownDescription": "HTTP/3 page masquerading.\n"
        }
      },
      "required": ["version", "auth", "masquerade"],
      "additionalProperties": false
    },
    "__schema438": { "type": "number", "const": 2 },
    "__schema439": { "default": 60, "type": "integer", "minimum": 0, "maximum": 9007199254740991 },
    "__schema440": {
      "markdownDescription": "If left blank, the default 404 page will be displayed.\n",
      "allOf": [{ "$ref": "#/definitions/__schema441" }]
    },
    "__schema441": { "default": "", "type": "string", "enum": ["file", "proxy", "string", ""] },
    "__schema442": {
      "markdownDescription": "Configuration items when type is file.\n",
      "allOf": [{ "$ref": "#/definitions/__schema443" }]
    },
    "__schema443": { "default": "", "type": "string" },
    "__schema444": {
      "markdownDescription": "Configuration items when type is proxy.\n",
      "allOf": [{ "$ref": "#/definitions/__schema445" }]
    },
    "__schema445": { "default": "", "type": "string" },
    "__schema446": {
      "markdownDescription": "Configuration items when type is proxy.\n",
      "allOf": [{ "$ref": "#/definitions/__schema447" }]
    },
    "__schema447": { "default": false, "type": "boolean" },
    "__schema448": {
      "markdownDescription": "Configuration items when type is proxy.\n",
      "allOf": [{ "$ref": "#/definitions/__schema449" }]
    },
    "__schema449": { "default": false, "type": "boolean" },
    "__schema450": {
      "markdownDescription": "Configuration items when type is string.\n",
      "allOf": [{ "$ref": "#/definitions/__schema451" }]
    },
    "__schema451": { "default": "", "type": "string" },
    "__schema452": {
      "markdownDescription": "Configuration items when type is string.\n",
      "allOf": [{ "$ref": "#/definitions/__schema453" }]
    },
    "__schema453": {
      "default": {},
      "type": "object",
      "propertyNames": { "type": "string" },
      "additionalProperties": { "type": "string" }
    },
    "__schema454": {
      "markdownDescription": "Configuration items when type is string.\n",
      "allOf": [{ "$ref": "#/definitions/__schema455" }]
    },
    "__schema455": {
      "default": 0,
      "type": "integer",
      "minimum": -9007199254740991,
      "maximum": 9007199254740991
    },
    "__schema456": { "type": "string", "const": "httpupgrade" },
    "__schema457": {
      "markdownDescription": "`HTTPUpgradeObject` corresponds to the `httpupgradeSettings` item in [StreamSettingsObject](https://xtls.github.io/en/config/transport.html#streamsettingsobject).\n\n```json\n{\n  // outbound example; also applies to inbound\n  \"outbounds\": [\n    {\n      // ...\n      \"streamSettings\": {\n        \"network\": \"httpupgrade\",\n        \"httpupgradeSettings\": {\n          \"acceptProxyProtocol\": false,\n          \"path\": \"/\",\n          \"host\": \"xray.com\",\n          \"headers\": {\n            \"key\": \"value\"\n          }\n        }\n      }\n    }\n  ]\n}\n```\n",
      "allOf": [{ "$ref": "#/definitions/__schema458" }]
    },
    "__schema458": {
      "type": "object",
      "properties": {
        "acceptProxyProtocol": {
          "markdownDescription": "Only used for inbound; indicates whether to accept PROXY protocol.\n\n[PROXY protocol](https://www.haproxy.org/download/2.2/doc/proxy-protocol.txt) is dedicated to passing the real source IP and port of the request. If you don't know what it is, please ignore this item for now.\n\nCommon reverse proxy software (such as HAProxy, Nginx) can be configured to send it. VLESS fallbacks xver can also send it.\n\nWhen set to `true`, after the underlying TCP connection is established, the requester must send PROXY protocol v1 or v2 first; otherwise, the connection will be closed.\n",
          "allOf": [{ "$ref": "#/definitions/__schema459" }]
        },
        "path": {
          "markdownDescription": "The HTTP path used by HTTPUpgrade. Default value is `\"/\"`.\n\nIf the client path contains the `ed` parameter (e.g., `/mypath?ed=2560`), `Early Data` will be enabled to reduce latency. The value represents the threshold for the first packet length. If the first packet length exceeds this value, `Early Data` will not be enabled. The recommended value is 2560.\n",
          "allOf": [{ "$ref": "#/definitions/__schema460" }]
        },
        "host": {
          "markdownDescription": "The host sent in the HTTP request of HTTPUpgrade. Default value is empty. If the server-side value is empty, the host value sent by the client is not verified.\n\nWhen this value is specified on the server, or specified in `headers`, it will verify whether it is consistent with the host requested by the client.\n\nPriority of the host sent by the client: `host` > `headers` > `address`.\n",
          "allOf": [{ "$ref": "#/definitions/__schema461" }]
        },
        "headers": {
          "markdownDescription": "Client-only. Custom HTTP headers. A key-value pair, where each key represents the name of an HTTP header, and the corresponding value is a string.\n\nDefault value is empty.\n",
          "allOf": [{ "$ref": "#/definitions/__schema462" }]
        }
      },
      "additionalProperties": false
    },
    "__schema459": { "default": false, "type": "boolean" },
    "__schema460": { "default": "/", "type": "string" },
    "__schema461": { "default": "", "type": "string" },
    "__schema462": {
      "default": {},
      "type": "object",
      "propertyNames": { "type": "string" },
      "additionalProperties": { "type": "string" }
    },
    "__schema463": { "type": "string", "const": "xhttp" },
    "__schema464": {
      "type": "object",
      "properties": {
        "host": { "default": "", "type": "string" },
        "path": { "default": "/", "type": "string" },
        "mode": {
          "default": "auto",
          "type": "string",
          "enum": ["auto", "packet-up", "stream-up", "stream-one"]
        },
        "extra": {
          "type": "object",
          "properties": {
            "headers": {
              "type": "object",
              "propertyNames": { "type": "string" },
              "additionalProperties": { "type": "string" }
            },
            "xPaddingBytes": {
              "default": "100-1000",
              "allOf": [{ "$ref": "#/definitions/__schema465" }]
            },
            "noGRPCHeader": { "default": false, "type": "boolean" },
            "noSSEHeader": { "default": false, "type": "boolean" },
            "scMaxEachPostBytes": { "allOf": [{ "$ref": "#/definitions/__schema465" }] },
            "scMinPostsIntervalMs": { "allOf": [{ "$ref": "#/definitions/__schema465" }] },
            "scMaxBufferedPosts": {
              "type": "integer",
              "minimum": -9007199254740991,
              "maximum": 9007199254740991
            },
            "scStreamUpServerSecs": { "allOf": [{ "$ref": "#/definitions/__schema465" }] },
            "xmux": {
              "type": "object",
              "properties": {
                "maxConcurrency": {
                  "default": "16-32",
                  "allOf": [{ "$ref": "#/definitions/__schema465" }]
                },
                "maxConnections": {
                  "default": 0,
                  "allOf": [{ "$ref": "#/definitions/__schema465" }]
                },
                "cMaxReuseTimes": {
                  "default": 0,
                  "allOf": [{ "$ref": "#/definitions/__schema465" }]
                },
                "hMaxRequestTimes": { "allOf": [{ "$ref": "#/definitions/__schema465" }] },
                "hMaxReusableSecs": { "allOf": [{ "$ref": "#/definitions/__schema465" }] },
                "hKeepAlivePeriod": {
                  "default": 0,
                  "type": "integer",
                  "minimum": 0,
                  "maximum": 9007199254740991
                }
              },
              "additionalProperties": false
            },
            "downloadSettings": {
              "oneOf": [
                {
                  "type": "object",
                  "properties": {
                    "security": { "$ref": "#/definitions/__schema353" },
                    "tlsSettings": { "$ref": "#/definitions/__schema354" },
                    "address": { "$ref": "#/definitions/__schema466" },
                    "port": { "$ref": "#/definitions/__schema467" },
                    "network": { "$ref": "#/definitions/__schema468" },
                    "xhttpSettings": { "$ref": "#/definitions/__schema469" },
                    "sockopt": { "$ref": "#/definitions/__schema470" }
                  },
                  "required": ["security", "tlsSettings", "address", "port", "network"],
                  "additionalProperties": false
                },
                {
                  "type": "object",
                  "properties": {
                    "security": { "$ref": "#/definitions/__schema169" },
                    "realitySettings": { "$ref": "#/definitions/__schema170" },
                    "address": { "$ref": "#/definitions/__schema466" },
                    "port": { "$ref": "#/definitions/__schema467" },
                    "network": { "$ref": "#/definitions/__schema468" },
                    "xhttpSettings": { "$ref": "#/definitions/__schema469" },
                    "sockopt": { "$ref": "#/definitions/__schema470" }
                  },
                  "required": ["security", "realitySettings", "address", "port", "network"],
                  "additionalProperties": false
                }
              ]
            }
          },
          "additionalProperties": {}
        }
      },
      "additionalProperties": false
    },
    "__schema465": {
      "anyOf": [
        { "type": "integer", "minimum": -9007199254740991, "maximum": 9007199254740991 },
        { "type": "string", "pattern": "^-?\\d+-\\d+$" }
      ]
    },
    "__schema466": { "type": "string" },
    "__schema467": { "type": "integer", "minimum": 0, "maximum": 9007199254740991 },
    "__schema468": { "type": "string", "const": "xhttp" },
    "__schema469": {
      "type": "object",
      "properties": {
        "host": { "default": "", "type": "string" },
        "path": { "default": "/", "type": "string" },
        "mode": {
          "default": "auto",
          "type": "string",
          "enum": ["auto", "packet-up", "stream-up", "stream-one"]
        }
      },
      "additionalProperties": false
    },
    "__schema470": { "allOf": [{ "$ref": "#/definitions/__schema297" }] },
    "__schema471": {
      "type": "object",
      "properties": {
        "enabled": { "default": true, "type": "boolean" },
        "destOverride": {
          "default": ["http", "tls", "fakedns"],
          "type": "array",
          "items": { "type": "string", "enum": ["http", "tls", "quic", "fakedns"] }
        },
        "metadataOnly": { "default": false, "type": "boolean" },
        "domainsExcluded": { "default": [], "type": "array", "items": { "type": "string" } },
        "ipsExcluded": { "default": [], "type": "array", "items": { "type": "string" } },
        "routeOnly": { "default": false, "type": "boolean" }
      },
      "required": ["destOverride"],
      "additionalProperties": false
    },
    "__schema472": {
      "type": "object",
      "properties": {
        "tag": { "$ref": "#/definitions/__schema142" },
        "listen": { "$ref": "#/definitions/__schema143" },
        "port": { "$ref": "#/definitions/__schema144" },
        "streamSettings": { "$ref": "#/definitions/__schema145" },
        "sniffing": { "$ref": "#/definitions/__schema471" },
        "protocol": { "type": "string", "const": "socks" },
        "settings": {
          "oneOf": [
            {
              "type": "object",
              "properties": {
                "udp": { "$ref": "#/definitions/__schema473" },
                "ip": { "$ref": "#/definitions/__schema474" },
                "userLevel": { "$ref": "#/definitions/__schema475" },
                "auth": { "type": "string", "const": "noauth" }
              },
              "required": ["auth"],
              "additionalProperties": {}
            },
            {
              "type": "object",
              "properties": {
                "udp": { "$ref": "#/definitions/__schema473" },
                "ip": { "$ref": "#/definitions/__schema474" },
                "userLevel": { "$ref": "#/definitions/__schema475" },
                "auth": { "type": "string", "const": "password" },
                "accounts": {
                  "default": [],
                  "type": "array",
                  "items": {
                    "type": "object",
                    "properties": {
                      "user": { "type": "string", "minLength": 1 },
                      "pass": { "type": "string", "minLength": 1 }
                    },
                    "required": ["user", "pass"],
                    "additionalProperties": false
                  }
                }
              },
              "required": ["auth"],
              "additionalProperties": {}
            }
          ]
        }
      },
      "required": ["port", "streamSettings", "protocol", "settings"],
      "additionalProperties": false
    },
    "__schema473": { "default": false, "type": "boolean" },
    "__schema474": { "type": "string" },
    "__schema475": { "default": 0, "type": "number" },
    "__schema476": {
      "type": "object",
      "properties": {
        "tag": { "$ref": "#/definitions/__schema142" },
        "listen": { "$ref": "#/definitions/__schema143" },
        "port": { "$ref": "#/definitions/__schema144" },
        "streamSettings": { "$ref": "#/definitions/__schema145" },
        "sniffing": { "$ref": "#/definitions/__schema471" },
        "protocol": { "type": "string", "const": "vmess" },
        "settings": {
          "type": "object",
          "properties": {
            "clients": {
              "type": "array",
              "items": {
                "type": "object",
                "properties": {
                  "id": { "type": "string", "minLength": 1 },
                  "level": { "type": "number" },
                  "email": { "type": "string" }
                },
                "required": ["id", "level", "email"],
                "additionalProperties": false
              }
            },
            "default": {
              "type": "object",
              "properties": { "level": { "type": "number" } },
              "required": ["level"],
              "additionalProperties": false
            }
          },
          "required": ["clients"],
          "additionalProperties": false
        }
      },
      "required": ["port", "streamSettings", "protocol", "settings"],
      "additionalProperties": false
    },
    "__schema477": {
      "type": "object",
      "properties": {
        "tag": { "$ref": "#/definitions/__schema142" },
        "listen": { "$ref": "#/definitions/__schema143" },
        "port": { "$ref": "#/definitions/__schema144" },
        "streamSettings": { "$ref": "#/definitions/__schema145" },
        "sniffing": { "$ref": "#/definitions/__schema471" },
        "protocol": { "type": "string", "const": "wireguard" },
        "settings": {
          "type": "object",
          "properties": {
            "secretKey": {
              "type": "string",
              "markdownDescription": "The WireGuard **private key** in base64 format.\n\n\nGenerate with:\n[View docs](https://www.wireguard.com/protocol/)",
              "description": "something xyz",
              "examples": ["mNb7OIe3sH4RfTkh..."]
            },
            "peers": {
              "type": "array",
              "items": {
                "type": "object",
                "properties": {
                  "publicKey": { "type": "string", "minLength": 1 },
                  "allowedIPs": { "type": "array", "items": { "type": "string", "minLength": 1 } }
                },
                "required": ["publicKey", "allowedIPs"],
                "additionalProperties": false
              }
            },
            "mtu": {
              "description": "MTU for the WireGuard interface.",
              "minimum": 576,
              "maximum": 65535,
              "allOf": [{ "$ref": "#/definitions/__schema478" }]
            }
          },
          "required": ["secretKey", "peers"],
          "additionalProperties": false
        }
      },
      "required": ["port", "streamSettings", "protocol", "settings"],
      "additionalProperties": false
    },
    "__schema478": { "default": 1420, "type": "number" }
  }
}
